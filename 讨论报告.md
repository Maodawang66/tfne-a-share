# 深度学习大作业：A 股趋势预测与模拟交易——讨论报告

> 本文档基于《深度学习基础大作业》PDF 全文、`data/README.md` 及对 `data/` 目录下样例文件的核对撰写；并结合金融统计、数学建模与深度学习文献与业界实践做调研归纳。  
> **正式实施方案（与 `项目方案.md` v2.0 一致）**：**档位 C — TFNE-C**（TFT + FT-Transformer + NewsLoRA + 多 seed 秩融合）。  
> 数据抽样核对：`basic.csv` 约 5500+ 只股票；`daily/`、`metric/`、`moneyflow/` 为按交易日截面 CSV；`news/` 为东方财富快讯（按日历日聚合），`stock_st/` 为 ST 名单；指数数据见 README。

---

## 一、作业要求解读（对照 PDF）

### 1.1 核心任务

- **预测对象**：利用过去一段时间的**特征序列**，预测股票**未来短期收益方向**（可具体化为未来 \(n\) 个交易日收益率或其符号）。
- **流程闭环**：数据处理与特征构造 → **必须包含神经网络**的模型设计与训练 → **策略评估与历史回测** → 同花顺模拟交易（FDL2026）→ 实验报告与反思。
- **股票池（比赛正式范围）**：除 **ST 股**、**北交所** 外的全部 A 股；训练阶段若算力或工程难度受限，可先子集（如沪深 300），但最终与比赛一致时建议对齐正式股票池（约 5000 只量级，README 与 `basic.csv` 一致）。

### 1.2 数据与时间约束（与 README 对齐）

| 数据类型 | 路径/文件 | 时间注意点 |
|----------|-----------|------------|
| 日频量价 | `daily/YYYYMMDD.csv` | 2016 起；按日截面，需自行 merge 成个股时序 |
| 基本面/估值/换手等 | `metric/` | 与量价同日对齐 |
| 资金流向 | `moneyflow/` | 与量价同日对齐 |
| 新闻快讯 | `news/` | **仅 2019 年起**；文本字段 `datetime, content, title` |
| ST 标记 | `stock_st/` | 2016-08 起 |
| 交易日历 | `trade_cal.csv` | 用于对齐、shift 标签、停牌/休市 |
| 指数 | `market/`、`index_weight/` | 作基准与可选成分池 |

**禁止未来信息**：特征与标准化只能用到决策日 \(t\) 及以前可获得的信息；标签应对齐为「在 \(t\) 收盘后可计算、在 \(t+1\) 开盘前可下单」的收益率（见**第五节**）。

### 1.3 推荐策略骨架（可扩展）

1. 首日等权买入 \(n\) 只股票；之后每日卖出持仓中**预测得分最低**的 \(k\) 只，同时买入全市场中**得分最高**的 \(k\) 只（\(n\in[5,30], k\in[1,5]\)）。
2. 比赛强调：**每日尽可能满仓**；A 股 **T+1**（当日买入次日方可卖）。

### 1.4 评价与回测（作业硬性/软性要求）

- **硬性**：训练/验证损失、**IC**、**ICIR**；报告规范、无泄露、可复现。
- **推荐（影响评分）**：历史回测——年化收益、夏普、最大回撤；与上证指数/沪深 300 等对比；有余力可加手续费、滑点、涨跌停、停牌、最小交易单位等约束。

---

## 二、数据字段与作业相关概念的经济学含义与使用注意

本章按 README 中**最小字段（列）**逐项说明：经济学直觉 + 建模/因果上需注意的点。未单列的表（如指数 `market/`）若字段与 `daily/` 一致，则与下列 OHLCV 等字段**同名同义**，区别仅在标的为**指数组合**而非单券。

### 2.1 `basic.csv`（证券主数据，慢变化或静态）

| 字段 | 经济学含义 | 注意点 |
|------|------------|--------|
| **ts_code** | 证券在数据集中的唯一标识（交易所后缀 `.SH`/`.SZ`/`.BJ` 区分上市地）。 | 与 `daily`/`metric`/`moneyflow` 等表的 join 键；**北交所 `.BJ`** 在正式比赛股票池外，训练与回测需与作业一致过滤。 |
| **symbol** | 交易所内简短数字代码，便于人工识别。 | 与 `ts_code` 信息冗余；文本匹配新闻时可能与简称并用，注意**前导零**与跨所重复口径。 |
| **name** | 证券简称，承载品牌与 ST 标记（如 `*ST`）等市场共识信息。 | 更名、重组会变；NLP 链指新闻依赖简称时需**维护别名表**或模糊匹配。 |
| **area** | 公司注册或总部所在地域，可作**区域经济周期、地方政策与产业集群**的代理。 | 与主营业务地未必一致；类别高基数时需编码/嵌入，警惕与行业**多重共线性**。 |
| **industry** | 行业分类，刻画**共同现金流因子与监管周期**（如周期、消费、科技）。 | 行业标签可能随数据源调整；用于**行业中性化**时注意 point-in-time（历史上行业是否被重新划分）。 |
| **cnspell** | 中文拼音缩写，检索与展示辅助。 | 对预测一般信息增量有限；可作弱特征或不用。 |
| **market** | 板块/市场层级（主板、创业板、科创板、北交所），决定**涨跌幅限制、投资者适当性、流动性结构**等制度差异。 | 与收益分布强相关；建模时要么显式作特征，要么在股票池规则中剔除北交所；避免与 `ts_code` 后缀**重复约束**时泄漏未来制度变更（少见）。 |
| **list_date** | 上市日期，公司进入公开市场融资的起点。 | **新股上市初期**量价与估值异常、历史窗口不足；需上市满 \(L\) 日再入池或单独处理；存活偏差讨论见**第八节 8.3**。 |
| **act_name** | 法律/披露意义上的实际控制人姓名或机构名。 | 反映**公司治理与股东背景**（国企/民企等）；文本高基、变更事件少，更适合规则过滤或主题标签而非强连续因子。 |
| **act_ent_type** | 实控人企业性质（央企、地方国企、民营等），与**预算软约束、融资约束、政策敏感度**相关。 | 类别不平衡；与 `industry`、`market` 联合易冗余；注意披露更新滞后。 |

### 2.2 `trade_cal.csv`（时间轴与交易制度日历）

| 字段 | 经济学含义 | 注意点 |
|------|------------|--------|
| **exchange** | 交易所（SSE 上交所、SZSE 深交所），决定**日历与部分产品规则**。 | 个股表用 `ts_code` 后缀已能判断交易所；日历表用于**对齐两所共同交易日**与节假日。 |
| **cal_date** | 自然日历日。 | 与 `trade_date` 对齐时，**非交易日**无截面行情；新闻文件按 `cal_date` 存时，需映射到**下一交易日**再与收益对齐。 |
| **is_open** | 当日是否为交易日（1 交易 / 0 休市）。 | 用于构造连续交易时间索引、shift 标签；休市日**禁止**伪造截面。 |
| **pretrade_date** | 上一交易日，用于链式计算「上一有效行情日」。 | 计算隔夜收益、缺失填充时比「日历减 1 天」更可靠；跨长假需注意。 |

### 2.3 `daily/` 与 `market/`（日频 OHLCV；指数与个股字段名一致）

| 字段 | 经济学含义 | 注意点 |
|------|------------|--------|
| **ts_code** | 标的代码；`market/` 中为指数代码（如 `000300.SH`）。 | 指数无 ST 问题；回测基准收益与个股**不可混表**除非明确对齐。 |
| **trade_date** | 该行情对应的交易日（YYYYMMDD）。 | 全表 join 主键之一；**标签永远是未来 trade_date** 的收益，严防错位一日。 |
| **open** | 开盘集合竞价与连续竞价初期形成的**开盘参考价**，反映隔夜信息与开盘流动性。 | 易受单笔大单影响；与收盘组合可刻画**日内模式**；停牌日缺失。 |
| **high** | 当日买方力量上界、流动性供给与涨跌停约束下的**最高价**。 | 极值对波动率估计敏感；与 low 联用算振幅；需结合涨跌停制度解读。 |
| **low** | 当日卖方压力下界。 | 同上；操纵或闪崩日会产生极端 low。 |
| **close** | 连续竞价结束时的收盘价，常作为**日收益核算基准**与 many 衍生品结算参考。 | 作业 PDF 默认用收盘价算收益；**复权**口径以数据源为准，`pre_close` 已按除权昨收。 |
| **pre_close** | 经除权除息调整后的**前一交易日收盘价**，用于连续可比价格序列。 | 计算 `pct_chg` 的分母；做手动收益时**优先用 close/pre_close-1** 与官方涨跌幅一致。 |
| **change** | 绝对涨跌额（价格单位），close 与除权昨收之差。 | 低价股与高价股不可比；截面建模多用收益率或 rank。 |
| **pct_chg** | 百分比涨跌幅，无量纲的**日收益代理**。 | 已考虑除权；接近涨跌停边界时分布截断；与其他表合并时检查**是否同一交易日**。 |
| **vol** | 成交量（手，1 手通常 100 股，以交易所规则为准）。 | 与股本结合才有「换手」经济含义；**停牌为 0 或缺失**；拆股历史需与股本数据一致（本 README 未单独给拆股表，依赖数据源已反映在价量中）。 |
| **amount** | 成交额（README：**千元**），反映**资金参与度与流动性深度**。 | **单位与 moneyflow（万元）不同**，融合前必须统一量纲；大单冲击日 amount 激增。 |
| **vwap** | 成交量加权平均价，近似「当日平均成交成本」的统计量。 | close 相对 vwap 偏离可解读为尾盘相对全日均价的**情绪或操纵**；指数同样适用。 |

### 2.4 `index_weight/`（指数成分及权重）

| 字段 | 经济学含义 | 注意点 |
|------|------------|--------|
| **index_code** | 标的指数（如沪深 300）。 | 用于构建**可投资股票池**或行业/风格 benchmark；成分调整日有**权重跳跃**。 |
| **con_code** | 成分股 `ts_code`。 | 与 `daily` 合并得到「某日是否属于指数」；做指数增强或限制池时需注意**调入调出滞后披露**。 |
| **trade_date** | 权重生效或快照日期（以数据源定义为准）。 | 严格回测应使用**当日已知成分**；若仅用静态当前成分回测历史，会引入**前视偏差**。 |
| **weight** | 该成分在指数中的权重（通常和为 1 或接近 1）。 | 可用于**加权基准收益**或约束组合偏离；权重过小股票噪声大。 |

### 2.5 `metric/`（估值、流动性与规模截面）

| 字段 | 经济学含义 | 注意点 |
|------|------------|--------|
| **ts_code** | 股票标识。 | 与 `daily` 按日 merge。 |
| **trade_date** | 交易日。 | 与 metric 同日 `close` 应对齐 `daily.close`。 |
| **close** | 当日收盘价（在 metric 表中重复存储）。 | 与 `daily` 冗余，merge 时避免重复列；不一致时应审计数据源。 |
| **turnover_rate** | 换手率（%），成交量相对流通股本的强度，**流动性与筹码交换**指标。 | 极端高换手可能是炒作或事件；新股、限售解禁期结构变化大。 |
| **turnover_rate_f** | 基于**自由流通股本**的换手率，更接近**可交易浮筹**的真实换手。 | 与 total 换手二选一或同时用需防共线；自由流通口径随指数公司调整变化。 |
| **volume_ratio** | 量比，当日成交相对近期均量的放大程度，**短期订单流异常**信号。 | 分母窗口定义依赖数据源；横截面比较需注意**停牌复牌**首日失真。 |
| **pe** | 市盈率（总市值/净利润），盈利估值倍数。 | **亏损为缺失**；极端 PE 可能因微利分母小；跨行业比较估值陷阱多。 |
| **pe_ttm** | 滚动四季度（TTM）盈利的 PE，平滑季节性与单次非经常损益。 | 仍对亏损缺失；盈利质量差时 TTM 仍误导。 |
| **pb** | 市净率（总市值/净资产），资产锚上的估值。 | 轻资产行业 PB 参考意义弱；净资产可为负（重组股）导致异常。 |
| **ps** | 市销率，收入视角的成长估值。 | 高成长、未稳定盈利公司常用；低利润率行业 PS 可比性差。 |
| **ps_ttm** | 滚动四季度营收的市销率。 | 同上，略平滑收入波动。 |
| **dv_ratio** | 股息率（%），过去分红相对价格的**现金回报**代理。 | 与**价值因子**相关；高送转、一次性特别股息会扭曲。 |
| **dv_ttm** | TTM 股息率。 | 更平滑；仍受股价波动分母影响。 |
| **total_share** | 总股本（万股），公司发行的股份总数上限概念。 | 用于换算市值、EPS；**单位万股**，与 `amount` 千元等同表时注意数量级。 |
| **float_share** | 流通股本（万股），理论上可在二级市场交易的部分。 | 与限售、战略配售相关；流通比例影响操纵难易。 |
| **free_share** | 自由流通股本（万股），剔除长期战略持仓等后的**可交易性更强**股本。 | 与指数权重、换手 `_f` 一致；数据源定义需统一。 |
| **total_mv** | 总市值（万元），价格 × 总股本的经济规模。 | **单位万元**；在因子中常取对数作规模因子；极端小市值流动性风险。 |
| **circ_mv** | 流通市值（万元），可交易部分的市值。 | 大资金容量约束更直接相关；与 total_mv 差异反映限售比例。 |

### 2.6 `moneyflow/`（按成交金额档位的订单流分解）

README 规定档位（按单笔成交额）：**小单为 5 万元以下；中单 5–20 万；大单 20–100 万；特大单不低于 100 万**；统计基于**主动买卖**。

| 字段 | 经济学含义 | 注意点 |
|------|------------|--------|
| **ts_code** | 股票标识。 | — |
| **trade_date** | 交易日。 | — |
| **buy_sm_vol** | 小单主动买入量（手）。 | 「小单」多被文献解读为**散户净主动**的粗代理，实为金额档而非账户类型；**不可过度叙事化**。 |
| **buy_sm_amount** | 小单主动买入金额（万元）。 | 与 vol 联用可核对数据内部一致性；**单位万元**，与 `daily.amount`（千元）区分。 |
| **sell_sm_vol** | 小单主动卖出量（手）。 | 买卖不对称反映该档位**净主动方向**。 |
| **sell_sm_amount** | 小单主动卖出金额（万元）。 | 同上。 |
| **buy_md_vol** | 中单主动买入量（手）。 | 介于机构与散户之间的**过渡订单**，可能是分单算法或中小机构。 |
| **buy_md_amount** | 中单主动买入金额（万元）。 | 与股价水平相关：同样手数在不同股价下金额档不同。 |
| **sell_md_vol** | 中单主动卖出量（手）。 | — |
| **sell_md_amount** | 中单主动卖出金额（万元）。 | — |
| **buy_lg_vol** | 大单主动买入量（手）。 | 常视为**机构或大单**净主动流入的粗代理；事件日与大单卖出一同解读。 |
| **buy_lg_amount** | 大单主动买入金额（万元）。 | 大市值股票日常大单绝对量天然更大，需**标准化**（如对 amount 或 circ_mv）。 |
| **sell_lg_vol** | 大单主动卖出量（手）。 | — |
| **sell_lg_amount** | 大单主动卖出金额（万元）。 | — |
| **buy_elg_vol** | 特大单主动买入量（手）。 | 更接近**机构 block trade** 或算法大单拆分后的尾部；低流动性股票样本稀疏。 |
| **buy_elg_amount** | 特大单主动买入金额（万元）。 | — |
| **sell_elg_vol** | 特大单主动卖出量（手）。 | — |
| **sell_elg_amount** | 特大单主动卖出金额（万元）。 | — |
| **net_mf_vol** | 全市场口径下的净流入量（手），买卖主动净额汇总。 | 需对照 README 精确定义（全档位合成）；与分项加总可做**一致性校验**。 |
| **net_mf_amount** | 净流入额（万元）。 | 常用于单因子排序；**与收益率的相关在截面上更稳定**若先标准化。 |

### 2.7 `news/`（非结构化快讯）

| 字段 | 经济学含义 | 注意点 |
|------|------------|--------|
| **datetime** | 新闻发布时间戳。 | **因果核心**：晚于当日收盘的新闻不应在「当日收盘决策」中无条件使用；应对齐到**下一交易日**或丢弃。与 `trade_cal` 联用。 |
| **content** | 正文，承载事件细节与语境。 | 重复转载、模板公告多；需去重与编码；长度差异大需截断或分层聚合。 |
| **title** | 标题，常为高度压缩的事件摘要。 | 与正文一并编码可提升召回；标题党噪声需规则过滤。 |

**表级注意**：新闻自 **2019** 起；为全样本联合建模需 **mask** 或与仅文本子模型分段训练（见**第六节**整体建模）。

### 2.8 `stock_st/`（风险警示证券清单）

| 字段 | 经济学含义 | 注意点 |
|------|------------|--------|
| **ts_code** | 被实施风险警示的证券代码。 | 正式比赛池**剔除**；历史上 ST 状态影响涨跌幅与流动性，回测也应剔除以与策略可比。 |
| **name** | 带风险标识的简称。 | 可与 `basic.name` 对照跟踪更名。 |
| **trade_date** | 该条 ST 状态记录的日期。 | ST **状态会变化**（戴帽/摘帽）；需用当日是否出现在表中判断，而非静态标签。 |
| **type** | 风险类型编码。 | 数据源枚举，需建映射表。 |
| **type_name** | 风险类型可读名称（如 ST、*ST 等细分）。 | 可用于分层分析不同风险成因；样本可能稀疏。 |

### 2.9 作业 PDF 中的市场、制度与交易术语

- **A 股**：中国大陆沪深交易所为主的人民币普通股票集合；本作业数据覆盖全体 A 股相关标的（比赛排除 ST 与北交所）。
- **ST 股**：被实施其他风险警示或退市风险警示的证券，定价与交易规则异于普通股票；**必须剔除**（亦见 2.8）。
- **北交所**：北京证券交易所上市公司，代码后缀 `.BJ`；作业正式池**剔除**。
- **沪深 300 / 上证综指 / 创业板指**：宽基或风格化**市场组合代理**，用于基准收益、风险因子与策略对比（数据在 `market/`）。
- **T+1**：当日买入份额**下一交易日**方可卖出；调仓逻辑必须与可卖持仓集合一致。
- **满仓**：资金尽可能全部持股；比赛公平性约束，与实务中「留现金」不同。

### 2.10 作业 PDF 中的收益、风险与回测评价术语

- **日频数据**：每个交易日至多一个观测；与 tick 数据相比信息集更粗但工程成本低。
- **短期收益方向**：模型输出的排序或符号对应未来 \(n\) 日（常 \(n=1\)）收益符号或相对基准的超额方向。
- **年化收益率**：将样本期收益换算到年化尺度便于比较；**极短样本年化不稳定**，报告需说明。
- **夏普比率**：单位波动下的超额收益，**风险调整绩效**指标；无风险利率常近似为 0 或短债利率。
- **最大回撤**：峰值到后续谷底的最大跌幅，刻画**尾部损失路径**与杠杆容忍度。
- **IC（信息系数）**：预测分数与未来收益的**截面相关系数**，衡量排序能力。
- **ICIR**：IC 均值与 IC 标准差之比，衡量**预测稳定性**而非单次运气。
- **方向胜率**：预测符号与实际收益符号一致的比例（可选指标）。

### 2.11 作业 PDF 中的建模、统计与作业用语

- **高噪声**：条件于公开信息，收益可预测部分弱、扰动强，导致估计方差大。
- **非平稳性**：边际分布、波动率聚集与结构断点随时间变化，违反经典平稳假设。
- **复杂依赖结构**：非线性、高阶交互、长记忆与截面相关（共同因子）并存。
- **特征标准化 / 归一化**：消除量纲、利于优化；**禁止用全样本未来信息**拟合 scaler（PDF 强调）。
- **特征工程**：从原始字段构造对新任务信息更集中的输入。
- **滑动窗口样本**：用连续 \(L\) 个交易日特征张量预测其后收益标签。
- **时间截面特征**：同一交易日内跨股票的可比变换（如截面 rank、分位）。
- **时间序列技术指标**（如 **MACD**、**RSI**）：由单券历史价格/成交量算出的**技术分析统计量**；经济学上多解释为动量/均值回复/过热的代理，统计上需防数据挖掘。
- **MLP**：多层全连接网络，非线性基线。
- **Transformer**：自注意力序列模型，捕获长程依赖；金融场景需强正则与防泄露窗口设计。
- **数据泄露**：特征或标准化中无意使用决策时尚不可知的信息，导致**虚高回测**。
- **Vibe Coding**：依赖 AI 生成代码而缺少人工审阅与测试；作业要求自行验证实现。

---

## 三、数据集结构与「全数据使用」的工程含义

### 3.1 基础量价（`daily/`）

字段经济学含义见**第二节 2.3**；工程上可构造：

- **收益与波动**：对数收益、已实现波动（Parkinson/Garman-Klass 等）、振幅。
- **流动性**：成交额、换手（亦可与 metric 交叉验证）、Amihud 非流动性等。
- **微观结构代理**：收盘价相对 VWAP 的偏离等。

### 3.2 进阶：基本面截面（`metric/`）

各字段见**第二节 2.5**；适合作为**慢变量**与估值锚，缓解纯价量过拟合噪声。

### 3.3 进阶：资金流（`moneyflow/`）

各字段见**第二节 2.6**；经济学直觉：**大单净主动买入**常被视为机构行为代理；需与市值/成交额标准化后使用，避免大市值天然大单多。

### 3.4 进阶：新闻（`news/`）

字段见**第二节 2.7**。样例显示为**全市场快讯**（宏观、行业、个股公告混排），**并非每只股票一条**。要「用到全部新闻」需：

1. **按时间截断**：对决策日 \(t\)，仅使用 \(\le t\) 且与交易决策时点一致可获得的新闻（见 2.7 `datetime`）。
2. **与股票关联**：规则/模型抽取实体（公司名、证券简称、`ts_code` 出现在文本中等）+ **语义检索**将新闻聚合到个股或行业；无法链接的可作为**市场层**或**行业层**因子。
3. **2016–2018 无新闻**：联合模型中应对该模态设 **mask**、**缺失指示变量**，或用仅价量子模型训练到 2018，再与 2019+ 多模态微调（见**第六节**整体建模）。

### 3.5 其它：`basic.csv`、`stock_st/`、`trade_cal.csv`、指数

- `basic`：字段见 2.1；`industry`、`market` 可作 **group 中性化**与行业 embedding。
- `stock_st`：见 2.8；与股票池过滤一致。
- `trade_cal`：见 2.2；对齐与 shift。
- `market` / `index_weight`：见 2.3、2.4；构建基准超额收益与可选成分池。

---

## 四、问题形式化与标签设计

### 4.1 预测目标（建议）

- **回归**：预测未来 \(n\) 日**超额对数收益**（相对市场或行业），或对收益做 **tanh / rank** 压缩以减轻极端值影响。
- **分类**：二分类「\(n\) 日收益是否大于 0（或是否跑赢基准）」；可采用 **focal loss** 处理类别不平衡。

\(n\) 可与调仓周期、比赛持有期一致（如 1、3、5）；需与回测再平衡频率一致以避免目标与执行错配。

### 4.2 截面 vs 时序

- 作业本质是**大横截面 + 短面板**：每日 4000–5000 只股票，各自 \(L\) 日历史窗口。
- 评价上业界常用 **截面 IC**（同日全股票预测值与未来收益的 Spearman/Pearson 相关），与 PDF 要求一致。

---

## 五、预处理与特征工程（强调无泄露）

本节放在**建模（第六节）与回测（第八节）之前**，原因是大截面股票机器学习里，**收益差异的很大一部分来自「管线是否干净」而非网络结构**；业界与顶刊文献反复强调：**前视偏差、错误的标准化、错误的交叉验证、以及多重检验下的数据窥探**，会把夏普与 IC 抬到「论文里很好看、实盘不可用」的水平。

### 5.0 文献与业界框架：为何「预处理」本身值得对标顶刊写法

1. **Gu, Kelly & Xiu（2020）*Review of Financial Studies***（下称 **GKX**）在「机器学习资产定价」设定下系统比较了线性模型、树与神经网络等，发现**树与神经网络**常占优，且重要信号与**动量、波动、流动性**等经济机制一致。该文配套工程与复现博客（如 Tidy Finance 对 GKX 的复现说明）指出：对大量公司特征，常见做法是**逐期（period-by-period）做截面秩变换**，再把秩线性映射到 **[-1, 1]**，使每个交易日上各预测变量具有可比尺度，并显式处理缺失秩（`NA` 不参与排序边界）。这与「在全样本上 fit 一次 StandardScaler」在**信息经济学含义上完全不同**：后者在数学上等价于使用了未来各期的联合分布信息，属于作业 PDF 明确禁止的泄露源之一。  
   - 论文信息：*Empirical Asset Pricing via Machine Learning*, RFS 2020；NBER 工作论文版本：https://www.nber.org/papers/w25398  
   - 复现中对秩变换的逐步说明（英文）：https://www.tidy-finance.org/blog/gu-kelly-xiu-replication/

2. **Giglio, Liao & Xiu（2021）*Review of Financial Studies***《Thousands of Alpha Tests》讨论资产定价中**海量检验与数据窥探（data snooping）**：当研究者对大量因子/策略做筛选时，「看起来显著」的比例会系统性偏高。其框架给出在大检验数下仍渐近有效的推断思路（含 wild bootstrap 等有限样本改进）。对本作业的启示是：**特征工程与模型搜索空间越大，越要在验证协议与报告里交代「试过多少设定、最终规则是否由单次 holdout 决定」**，避免把网格搜索等价成「无成本的信息」。  
   - 常用入口：SSRN https://papers.ssrn.com/sol3/papers.cfm?abstract_id=3259268  

3. **Feng, Giglio & Xiu（2020）*Journal of Finance***《Taming the Factor Zoo: A Test of New Factors》从**因子动物园**角度讨论大量候选因子/预测变量下的检验与冗余问题，与「高维特征 + 机器学习」场景直接相关：预处理阶段若不做**经济含义约束、共线性处理、或 shrinkage**，模型容易拟合到「样本内可解释、样本外漂移」的虚假交互。  
   - https://doi.org/10.1111/jofi.12927  

4. **Kozak, Nagel & Santosh（2020）*Journal of Financial Economics***《Shrinking the Cross-Section》强调在横截面预期收益估计中使用**收缩（shrinkage）/贝叶斯先验**以降低估计误差与噪声交易；工程上对应：**对极端预测做收缩、对因子暴露做先验约束、或在组合层做风险模型收缩**。这与「winsorize 预测值 / 对权重加 caps」是同一类思想在不同层的体现。  
   - https://doi.org/10.1016/j.jfineco.2019.07.008  

5. **Mekelburg & Strauss（2024）*Journal of Empirical Finance***《Pooling and winsorizing machine learning forecasts to predict stock returns with high-dimensional data》（DOI：**10.1016/j.jempfin.2024.101538**）发现：单一 ML 模型预测误差可能很大并导致「预测失败」，但对多模型预测做 **winsorize 后再 pooling（集成）**，在多国市场样本上可获得更一致的样本外可预测性；并讨论稀疏/稠密结构随时间演化。对本作业启示：**集成 + 对预测尾部的稳健处理**不是「技巧」，而是近年实证金融里被系统验证过的稳定增益来源之一。  
   - RePEc/IDEAS 摘要页：https://ideas.repec.org/a/eee/empfin/v79y2024ics0927539824000732.html  

6. **López de Prado（2018）《Advances in Financial Machine Learning》**（业界量化经典教材）系统提出 **Purged K-Fold / Combinatorial Purged Cross-Validation（CPCV）** 等：当标签是未来 \(h\) 日收益时，训练集与验证集样本在时间上**重叠的标签窗口**会泄漏信息，需要在折间 **purge（清除）** 并用 **embargo（禁运期）** 隔断相邻折。深度学习若仍用「随机打乱 mini-batch 当作 i.i.d.」而不处理面板结构，会低估方差、抬高泛化错觉。  

7. **关于「泄露如何量化」的警示性研究**：近年有工作以仿真/实证方式展示**滚动归一化窗口若向前污染数日**、或误用 **K 折交叉验证** 时，夏普等绩效指标可被大幅抬高；这类结论用于强化「必须用时间顺序验证」的纪律（具体作者与版本以你最终引用文献库为准，可在 Research Square / SSRN 以 *financial machine learning data leakage* 检索核对）。  

8. **Kakushadze（2016）arXiv:1601.00991《101 Formulaic Alphas》**代表业界因子工厂文化：用有限算子库在 OHLCV 上生成大量候选信号；其启示是「**先构造经济含义清晰、可复现的算子特征**，再交给神经网络做非线性组合」，往往比直接把原始五价直接塞入网络更稳、更易写报告。  

下文按「作业 PDF 要求链条」展开：**信息集 → 样本与标签 → 时间切分/验证 → 缺失与表连接 → 极值与稳健缩放 → 截面标准化/中性化 → 特征构造 → 文本 → 窥探控制 → 自查清单**。

### 5.1 信息集、决策时点与「可交易标签」对齐（最优先、最细）

- **信息集定义**：在交易日 \(t\) 的收盘决策问题中，所有特征只能依赖「在 \(t\) 日收盘前（或数据供应商定义的盘后固定时点前）理论上可获得」的信息。你们数据为日频盘后更新场景时，应对齐为：用 \(\le t\) 的 `daily/metric/moneyflow` 与按规则截断后的新闻，预测 **从 \(t\) 收盘到未来可交易实现** 的收益（常见为 \(t+1\) 收盘相对 \(t\) 收盘，或 \(t+1:t+n\) 复利收益）。  
- **严禁混用「日历日」与「交易日」**：`news/` 按自然日文件存储时，若新闻时间晚于交易所收盘，应映射到 **下一交易日** 再进入该日的特征，否则属于「用到了当时尚未进入投资者信息集」的灰区泄露。  
- **复权与收益率**：优先使用 `pct_chg` 或 `close/pre_close-1` 与数据源一致；跨表构造的收益率必须保证 **同一 `trade_date` 语义**（合并键加断言）。  
- **重叠标签（overlapping labels）**：若 \(n>1\)，相邻训练样本的未来收益窗口在时间上重叠，标准 K 折会让验证折「看到」与训练折共享的未来段；应按 López de Prado 思路在折间删除重叠区间或使用 purged CV。  
- **对齐比赛 T+1**：标签定义与「哪一天允许卖出」共同决定可执行收益；预处理阶段就要把「预测 horizon」与「调仓频率」写入同一张设计表，避免模型学 \(n=5\) 但策略每天换仓的错配。

### 5.2 时间切分、训练/验证/测试协议：walk-forward、purge、embargo

- **禁止**：把全时段样本随机打乱后做 K 折；把「全历史 mean/std」用于标准化；在验证集上反复调参后仍声称验证集是「样本外」。  
- **推荐基线**：**滚动或扩展窗口（walk-forward）**：用 \([0,T_{\text{train}}]\) 训练，在 \((T_{\text{train}}, T_{\text{train}}+H]\) 验证；时间向前滚动。验证集只做**一次或少次**模型族选择；最终测试用比赛期或保留的最后一段。  
- **进阶**：对树模型与神经网络，使用 **Purged + Embargo** 的交叉验证折，使相邻折之间不仅「训练在前、验证在后」，还剔除标签窗口重叠与边界泄漏（AFML 第 7 章框架）。  
- **多模态时间不对齐**：新闻自 2019 起，建议构造 **缺失指示变量** 或分段训练（先 2016–2018 纯数值预训练，再 2019+ 联合微调），并在报告中报告 **两段样本的指标对比**，避免读者误以为全期都有文本。  
- **与 GKX 的 out-of-sample 叙事一致**：顶刊资产定价论文通常强调「训练窗滚动 + 严格 OOS」，这不是形式主义，而是控制 **regime shift（结构突变）** 下模型自由度过高的问题；Rossi（2021）*Journal of Economic Literature* 对预测不稳定性的综述也可作为「为何时间切分比调参更重要」的理论支撑。

### 5.3 样本构造：股票池、ST/北交所、停牌、新股与「可交易集合」

- **股票池**：与比赛一致时，应在特征层就剔除 **ST**（`stock_st/` 当日命中）与 **北交所**（`ts_code` 后缀 `.BJ` 或 `basic.market`）。若研究子池（沪深 300），必须用 `index_weight/` 的 **point-in-time 成分** 过滤，避免「用 2025 成分回测 2018」。  
- **停牌与零成交**：停牌日常表现为 `vol=0` 或缺失；应用 `trade_cal` 确认是否交易日。对停牌日：要么剔除该股票该日样本，要么用 **mask** 让网络显式学习缺失模式；**不要用未来复牌后的价格向前填充**当作当日特征。  
- **新股与短历史**：上市不足 \(L\) 窗长的股票要么不入训练，要么用 **较短窗口 + 填充掩码**；否则尾部小票会带来「伪序列」与幸存者偏差讨论（见第八节 8.3）。  
- **横截面规模不平衡**：每日可交易股票数波动；若某日有效样本极少，截面秩/标准化方差不稳定，可设最小截面样本阈值（如少于 200 只则跳过该日或只用全局缩放）。

### 5.4 缺失值处理：从「填数」到「可识别的缺失机制」

- **价格/成交额缺失**：区分「真缺失」与「停牌」；二者经济机制不同。深度学习可用 **可学习缺失指示**（missing indicator channel）或 **NaN-aware** 归一化（先秩变换再对缺失单独编码）。  
- **PE 等估值缺失**：亏损公司常见；**不要用全截面均值填充**掩盖经济状态。推荐：**缺失指示 + 条件填充（行业内中位数）** 或 **两阶段模型**（先预测是否盈利再预测收益）。GKX 式秩变换对 `NA` 的处理要点是：缺失不参与秩的极值归一，避免把「缺失」错误变成「最大/最小」。  
- **资金流与量纲**：`moneyflow` 金额单位为**万元**，`daily.amount` 为**千元**；合并前必须统一，否则网络学到的是「单位 artefact」而非经济信息。  
- **新闻缺失**：无新闻日不等同于「零情感」，而应等价于 **文本模态缺失**，由门控网络降权（见第六节）。

### 5.5 极值与重尾：截面 winsorize、稳健目标与「预测 winsorize」

- **截面 winsorize（推荐默认）**：对每个 `trade_date`、每个特征，在截面上将分位数以外（如 0.5%–99.5% 或 1%–99%）截断。目的不是「造假」，而是降低**微盘股、数据错误、极端事件**对梯度与相关估计的支配；与 Mekelburg & Strauss（2024）对 **预测值 winsorize** 的思想一致：金融收益重尾下，极端预测往往是噪声放大器。  
- **时间序列 winsorize（谨慎）**：仅在**因果滚动窗**内对单只股票历史做截断，避免用未来分位点。  
- **标签处理**：可对超额收益做 **tanh 压缩**或分位数映射，减轻 MSE 被极端日主导；分类任务可用 **focal loss** 处理涨跌不平衡。  
- **对数化**：对 `total_mv`、`amount`、成交额类右偏变量常用 \(\log(1+x)\)，但注意 zeros 与缺失的定义顺序（先处理缺失与停牌，再 log）。

### 5.6 标准化与截面可比：GKX 式秩映射、z-score、分位数与「绝不用全样本 scaler」

- **GKX / Tidy Finance 复现中强调的秩映射（经典做法）**：在每个交易日 \(t\)，对同一特征在截面上排序，将秩线性变换到 **[-1,1]**，使「当天最大」映射到上界、「当天最小」映射到下界；**逐期**重复。其优点：  
  - 对单调变换稳健，适合后面接树或神经网络；  
  - 与 **IC / 排序选股** 目标一致（预测的是相对排序而非绝对价格尺度）；  
  - 对重尾分布比 z-score 更不易被单日极端值绑架。  
- **截面 z-score（替代或补充）**：在 winsorize 后，按日减截面均值除以截面标准差；若当日截面方差极小，需加 **数值下限** 或对标准差做收缩（与 Kozak et al. 的 shrinkage 思想一致）。  
- **滚动训练窗 z-score（用于部分时序模块）**：仅用 \([t-W,t]\) 内已实现的收益/波动统计量估计均值方差，再标准化当前输入；**W 的右端点不得跨过决策日之后**。  
- **明确禁止**：`sklearn.preprocessing.StandardScaler().fit(全时段)` 后 transform 各年；或 BatchNorm 在「全历史」上估计 running statistics 后声称无泄露——这在严格因果意义下不成立。若使用 BatchNorm，更合理的是在**每个训练折内**重新估计，或在网络中用 **LayerNorm / GroupNorm** 降低对全局统计依赖。  
- **行业/规模中性化的前置**：中性化常在秩/z-score **之后**或对原始经济变量做回归残差；实现上可用「行业内秩」两步法近似，降低计算量。

### 5.7 行业、规模与风险因子中性化（Barra / Fama-MacBeth 思想的工程化）

- **行业中性化**：对行业虚拟变量或行业固定效应做回归，取残差作为因子输入；或对每个特征做「减行业当日均值」。目的：减少预测分数只是「押注某一行业 beta」的概率。  
- **规模（log市值）中性化**：小市值溢价会使模型「永远偏小票」；常用 WLS 回归（权重与市值或波动相关）或直接在损失里加 **规模暴露惩罚**。  
- **市场 beta 中性（可选）**：用滚动 60 日或 120 日对市场收益回归得 \(\beta\)，将收益或特征对 \(\beta\) 正交化；实现复杂但报告加分。  
- **与 Feng et al.（2020）一致的态度**：中性化不是「去掉 alpha」，而是把**可交易的 alpha**与**已知风险补偿来源**分离，使验证集上的 IC 更接近「可复现的异质性信息」。

### 5.8 特征构造：价量算子、Alpha 工厂与「文献锚定的信号族」

- **价量算子层**：收益率（1/5/10/20 日）、波动（Parkinson/日内振幅）、成交量突增、Amihud 非流动性、价格相对 VWAP 偏离、最高最低相对收盘的位置等。  
- **WorldQuant 101 风格**：用 `tsrank`、`tsdelay`、`tsdelta`、`tscorr` 等算子在面板内构造大量候选，再做截面 rank；适合作为 **CNN/Transformer 前的显式因子层** 或作为 **树模型特征**。  
- **GKX 文结论导向**：在解释变量重要性时，优先检查是否主要落在 **动量、波动、流动性** 族；若重要性集中在「难以解释的高频交互」，要怀疑过拟合或泄露。  
- **资金流特征**：在统一量纲后，构造「大单净流入 / 成交额」「特大单买卖不对称」等比率特征，再做截面秩；避免 raw volume 级别特征被大市值支配。  
- **指数共同因子**：从 `market/` 构造市场波动率（如 HS300 20 日实现波动）、趋势状态，作为**全局 conditioning 变量**输入网络或作为中性化回归里的控制变量。

### 5.9 文本模态预处理（与数值管线同样严肃）

- **清洗**：HTML 标签、重复空格、全角半角统一；繁简转换；去除明显无关模板段。  
- **去重**：同一公告多源转载可用 SimHash/MinHash + 时间窗；避免同一信息被计数三次导致情感「虚假放大」。  
- **时间因果**：见 5.1；与 `trade_cal` 合并做 **event time → trade_date** 映射。  
- **表征（档位 C）**：`hfl/chinese-roberta-wwm-ext` + **LoRA 微调**（PEFT）；长文本用分段 + **注意力池化**；2019 年前样本用 `has_news` 掩码。可辅以事件类型多任务（可选）。依据 FinBERT / NewsLoRA 类文献（arXiv:1906.09024、2306.02136 等）。  
- **与数值融合前**：建议文本分支单独输出 **低维 embedding**，再与数值拼接；并对无新闻样本保留 **mask**，避免把「无文本」学成「极端利空」。

### 5.10 数据窥探、多重检验与「报告纪律」（与 Giglio–Liao–Xiu 框架对齐）

- **因子/特征搜索**：若尝试了上百个交互特征，最终只报告最好的一个，本质上属于多重检验；应在附录列出 **搜索空间规模** 或使用更保守的阈值解释。  
- **Giglio, Liao & Xiu（2021）**给出的方向性启示：把「检验数很大」纳入推断框架；本作业虽非正式学术发表，但评分上**诚实披露调参路径**通常优于「只给最优结果」。  
- **基准模型**：至少保留 **FT-Transformer-only（缩小版）** 或 **线性回归 + 行业固定效应** 作为对照，证明 TFNE-C 增益不是来自某段偶然区间（与 GKX 对照精神一致）。  
- **稳定性诊断**：除 IC 均值外，必须跟踪 **IC 时间序列**与分市场阶段（牛/熊/震荡）表现；Rossi（2021）强调预测模型在结构突变下会失效，报告中讨论这一点是加分项。

### 5.11 与本作业 PDF 条款逐项对照（提交前自查）

| PDF / 数据要求 | 预处理层对应动作 |
|----------------|------------------|
| 禁止随机打乱时间 | mini-batch 内可按日分组；评估用时间 walk-forward。 |
| 禁止全样本标准化 | 采用逐日截面秩/截面 z-score 或滚动窗估计 scaler。 |
| 禁止未来信息 | 新闻时间戳映射；指数成分 point-in-time；标签 shift 正确。 |
| 缺失与异常 | winsorize + 缺失指示；停牌/新股规则写清。 |
| 进阶数据单位 | `amount` 千元 vs `moneyflow` 万元统一。 |
| 可复现 | 随机种子、版本号、`requirements.txt`、特征列表冻结。 |

### 5.12 初学者导读：这些论文/做法在说什么，以及你们实验里「一步步该怎么做」

下面用**尽量少公式**的方式，把第五节里出现的文献与业界习惯，翻译成「时间序列 + 深度学习 + 特征工程」初学者能跟上的语言，并**逐项对应到你们 A 股大作业的数据与代码任务**。

#### （1）先建立一个心智模型：你们做的不是「单只股票 K 线预测」

- **普通时间序列课**常讲：一条序列 \(x_1,x_2,\ldots\)，预测下一步 \(x_{t+1}\)。  
- **你们的数据**是：每个交易日 \(t\)，横截面上有 **几千只股票**；每只股票又有一段 **过去 \(L\) 天** 的历史。这叫**面板数据（panel）**：「时间 × 股票」二维网格。  
- **业界与 GKX 类论文的预测对象**往往是：在 \(t\) 时刻，用所有股票的可得特征，去解释或预测 **下一期（或未来几期）收益的横截面关系**；评价常用 **IC**（排序相关），而不是单序列 MSE。

**你们实验里**：写代码时脑子里要有一张表：行 = `trade_date`，列 = `ts_code`，格子里是特征或标签；模型每天对「当日所有股票」吐一个分数向量。

#### （2）GKX（2020）：机器学习在「很多公司特征」上能不能定价？

- **他们的问题**：给定几十到上百个公司层面的预测变量（类似你们的 `metric` + 价量衍生特征），能不能用非线性模型更好地预测未来收益或风险溢价？  
- **他们的核心做法（与你们最相关的是预处理）**：对特征做**逐日截面处理**——每个交易日各自排序、映射到可比区间（复现博客里常写成映射到 **[-1,1]**），让神经网络/树「每天吃到的输入尺度一致」，且**不依赖全样本的全局均值方差**。  
- **为什么重要**：股价、成交额、市值的量纲差几个数量级；神经网络对尺度敏感。若用「全期 fit 的 StandardScaler」，等于偷偷告诉模型「未来分布长什么样」，作业里算**泄露**。  
- **他们论文结论（你们要会复述）**：树与神经网络往往强，重要信号常落在**动量、波动、流动性**附近——这给你们写「经济含义」段落时可以直接对照自己的特征重要性图。

**你们实验里怎么做（可写进报告「实现步骤」）**：

1. 从 `daily/` merge 出每只股票每日行情；按作业过滤 ST、北交所。  
2. 构造一批特征列（收益率滞后、波动、换手、资金流比率等，见 5.7–5.8）。  
3. **对每个 `trade_date`，在当日所有股票截面上**，对每个特征做 winsorize（可选）→ **秩变换**到 [-1,1]（或先做截面 z-score）；`PE` 等缺失用「不参与排序边界」的规则处理。  
4. 标签例如：`fwd_ret = close[t+1]/close[t]-1` 或相对沪深 300 的超额收益；**保证**特征只用 \(\le t\) 的信息。  
5. 训练 **TFNE-C 三模态模型**（或先跑通 **FT-Transformer-only** 缩小版作对照），再报告 IC/回测——与 GKX「多模型对照」精神一致。

#### （3）Giglio–Liao–Xiu（2021）：不是模型不好，是「试太多次」会假阳性

- **直觉**：如果你抛硬币 1000 次，总会有一段「看起来很像有规律」；因子挖掘也一样，试 500 个特征组合，最好的那个可能只是运气。  
- **论文贡献（你们不用会推导）**：在「检验次数巨大」时，如何控制**错误发现（false discovery）**，让「显著」更可信。

**你们实验里怎么做**：

1. 在报告里诚实写：尝试过哪些特征子集、哪些窗口 \(L\)、哪些模型；**最终提交的配置**是依据哪一段**验证期**选的。  
2. 不要把「在整段历史上网格搜索到最优」再拿同一段历史当「样本外」吹嘘；至少保留 **最后一年/一季完全没参与选参** 的曲线或指标。  
3. 若课程允许，可附录一张「调参次数 vs 性能」说明，体现你们理解 **Giglio–Liao–Xiu（2021）** 对多重检验的担忧。

#### （4）Feng–Giglio–Xiu（2020）「因子动物园」：特征太多时，很多是重复的

- **直觉**：一百个因子可能只有十个独立方向，其余是线性组合或噪声。  
- **对深度学习的含义**：网络容量大时，会把噪声拟合成「看起来很酷的交互」。

**你们实验里怎么做**：

1. 特征不要盲堆：先分族（动量/波动/流动性/估值/资金流/文本），每族保留若干代表。  
2. 训练后看 **相关性矩阵 / VIF / 简单 PCA 方差贡献**（写报告即可，不必上复杂检验），解释你有没有去掉明显共线的列。  
3. 与 **简单模型（少特征 MLP）** 对比，证明复杂模型不是纯过拟合。

#### （5）Kozak–Nagel–Santosh（2020）收缩：预测太「极端」往往是噪声

- **直觉**：横截面上估计「谁明天涨最多」误差很大；把预测往 0 或往市场均值**拉一点**，组合反而更稳。  
- **论文是理论+资产定价**，你们工程上对应：**对模型输出做 winsorize**、或 **对权重加 cap**、或 **训练目标加 L2**。

**你们实验里怎么做**：在得到每日每只股票的 `raw_score` 后，在截面上对分数做 1%/99% 截断，再送入调仓函数；对比截断前后 IC/IR 与回测夏普（与 Mekelburg & Strauss 2024 的「预测层 winsorize + 集成」叙事一致）。

#### （6）Mekelburg & Strauss（2024）：单模型会「崩」，集成 + 稳健化更像业界

- **他们发现了什么**：很多 ML 模型单独预测收益会阶段性完全失效；把多个模型的预测 **pooling**，并对极端预测 **winsorize**，样本外更稳。  
- **初学者理解**：就像「多个弱学习器投票」比「押宝一个天才模型」更不容易翻车。

**你们实验里怎么做**：训练 **TFT + FT-Transformer + NewsLoRA** 三个深度子模型，**3 个随机 seed** 训练后对预测 **winsorize**，再 **秩融合** 得到最终 `score`（与第六节 TFNE-C 一致）。

#### （7）López de Prado（AFML）：标签是未来几天收益时，交叉验证不能乱折

- **直觉**：若标签是「未来 5 日累计收益」，相邻两天的训练样本标签窗口**重叠了 4 天**，随机 K 折会让训练集「偷看」验证集标签里共享的未来信息。  
- **做法名字记住即可**：**Purged CV**（折间删掉重叠区间）、**Embargo**（验证折后再空一段训练不用）。

**你们实验里怎么做**：若 \(n>1\) 日标签，报告里说明验证方案是 **简单时间切分** 还是 **purged**；初学者至少做到：**绝不**对 `(股票,日期)` 样本做随机 K 折。短期 \(n=1\) 也要养成习惯，写清「训练窗结束日 < 验证窗起始日」。

#### （8）101 Alphas：业界「因子工厂」——用算子批量生产候选信号

- **直觉**：不是手工想 3 个特征，而是用 `delay、delta、rank、corr` 等小积木在 OHLCV 上**程序化拼**出很多候选，再筛选。  
- **对深度学习的分工**：Alpha 负责提供「**经济含义清晰、可复现**」的输入；网络负责学**非线性组合**。

**你们实验里怎么做**：先实现 10–30 个算子特征（动量、反转、振幅、量比变化），跑通 IC；再把这些列喂给 **TFT**（\(L=60\)），比直接 raw OHLC 更容易写报告、也更容易 debug。

#### （9）业界「Barra / 风险模型」口吻：行业、市值中性不是玄学

- **直觉**：你发现模型总是买「某一行业」或「小市值」，可能不是 alpha，而是**风险暴露**（行业 beta、小盘溢价）。中性化 = 先把这些已知方向从特征或预测里**扣掉**，再看还剩什么。

**你们实验里怎么做**：对每个交易日，每个特征减 **行业均值**（`basic.industry`），或对预测分数做「对行业虚拟变量回归取残差」；市值用 `log(total_mv)` 同理。报告里放一张「中性化前后行业暴露」对比图会很好看。

#### （10）小结：你们第五节「最小可交付」清单

1. 统一 `trade_date` 与 `ts_code` 主键；单位换算（千元/万元）。  
2. 逐日截面 winsorize + 秩或 z-score；**禁止**全样本 StandardScaler。  
3. 标签 `shift(-1)` 或显式 `t+1` 收益；新闻时间对齐。  
4. 时间切分训练/验证；可选多模型集成 + 预测 winsorize。  
5. 报告写清：对照模型、搜索空间、是否中性化——对应 **Giglio–Liao–Xiu（2021）** 与 **Feng–Giglio–Xiu（2020）** 所强调的「透明搜索空间 + 审慎推断」态度。

---

## 六、整体建模方案（档位 C）：TFNE-C 三深度子模型 + 多 seed 秩融合

以下为本项目**正式采用**的档位 C 方案（与 `项目方案.md` v2.0 一致），满足：**神经网络必选**、用尽价量/基本面/资金流/新闻/指数，且预处理无泄露。

**方案名称**：**TFNE-C** = **T**FT + **F**T-Transformer + **N**ews**L**oRA + 多 **s**eed 秩融合。

### 6.1 模态 A——时序主干：Temporal Fusion Transformer（TFT）

| 项 | 规格 |
|----|------|
| 输入 | 过去 **\(L=60\)** 日、约 **20 维**价量/资金流特征（第五节秩变换后） |
| 静态协变量 | `industry_id`, `log_mv`, `listing_age_days`（仅用截至 \(t\) 信息） |
| 结构 | `hidden_size=128`, `lstm_layers=2`, `attention_head_size=4`, `dropout=0.2` |
| 实现 | `pytorch-forecasting` 或等价 TFT 模块 |
| 输出 | \(z^A_{i,t}\)（预测 `excess_ret_1d` 的 Huber 损失） |
| 依据 | Lim et al. (2021, *IJF*); GKX (2020) |

**注意**：不使用「未来宏观路径」作为 TFT 已知未来输入；`mkt_vol` 仅取截至 \(t\) 的历史。

### 6.2 模态 B——截面主干：FT-Transformer

| 项 | 规格 |
|----|------|
| 输入 | 当日 `metric` + 资金流比率 + `industry` embedding（已逐日截面秩变换） |
| 结构 | `d_token=128`, `n_blocks=4`, `attention_dropout=0.2` |
| 实现 | `rtdl.FTTransformer` 或 `pytorch-tabular` |
| 输出 | \(z^B_{i,t}\) |
| 依据 | Gorishniy et al. (NeurIPS 2021); GKX (2020) |

### 6.3 模态 C——新闻主干：RoBERTa-wwm + LoRA（NewsLoRA）

| 项 | 规格 |
|----|------|
| 骨干 | `hfl/chinese-roberta-wwm-ext` |
| 微调 | **PEFT LoRA**（`r=8`, `alpha=16`），非全参数冻结 |
| 聚合 | 链指 `ts_code` 后 **注意力池化**；无新闻 → `has_news=0` |
| 市场层 | 无法链指新闻 → `market_news_score`（全市场共享） |
| 输出 | \(z^C_{i,t}\) |
| 依据 | Hu et al. (2022) LoRA; FinBERT 类实证 |

### 6.4 模态 D——市场状态（门控，非独立大模型）

- `market/000300.SH` → `mkt_vol`（20 日滚动波动）、可选趋势项。  
- 拼入 **融合门控 MLP**：\([z^A,z^B,z^C,mkt_vol,has\_news]\) → softmax 权重（验证集训练）。

### 6.5 多 seed 训练与秩融合（Mekelburg & Strauss, 2024）

1. **随机种子**：`{42, 123, 456}`（可增至 5），每 seed 完整训练 A/B/C。  
2. **预测 winsorize**：各子模型验证集输出截面 1%/99% 截断。  
3. **seed 内平均**：\(\bar z^m = \text{mean}_s z^m_s\)。  
4. **秩融合**：\(s_{i,t} = \sum_m w_m \cdot \text{Rank}(\bar z^m_{i,t})\)，\(w_m \propto \max(ICIR_m,0)\) 在验证集估计。  
5. **可选门控**：动态权重与静态 \(w_m\) 在报告中做消融。

### 6.6 初学者导读：档位 C 怎么落地

#### （1）与第五节的关系

先跑通 **Parquet 面板 + 逐日秩变换**，再训 TFT/FTT/NewsLoRA；否则 GPU 再强也会学到泄露噪声。

#### （2）TFT 在学什么

过去 60 日的价量/资金模式 + 行业/市值静态信息 → 预测下一日**超额收益**。报告可附 **variable importance** 图。

#### （3）FT-Transformer 在学什么

同一交易日内，哪只股票估值/换手/资金流相对更「便宜或过热」→ 截面排序信息，与 TFT 的「时间记忆」互补。

#### （4）NewsLoRA 在学什么

新闻文本中的事件与情绪 → 链指到个股后的日度文本 alpha；2019 年前自动 mask。

#### （5）多 seed 为什么要做

降低「一次训练运气好」的风险；融合后方差更小，对应 Mekelburg & Strauss (2024) 的 pooling 思想。

#### （6）消融实验（报告必做）

| 实验 | 内容 |
|------|------|
| TFT-only ×3 seed | 时序贡献 |
| FTT-only ×3 seed | 截面贡献 |
| NewsLoRA-only | 文本贡献（2019+） |
| **TFNE-C 全模态** | **主结果** |
| 线性回归 + 行业 FE | 弱对照 |

#### （7）算力与云资源（long 方案）

分阶段：`01 面板(CPU)` → `02 新闻向量/LoRA(GPU)` → `03 TFT/FTT×seed(GPU)` → `04 融合回测`。单次最长 72h；详见 `项目方案.md` 第十六节。

#### （8）本地 smoke test（仍为档位 C 缩配）

`config.yaml` 中 `smoke_test: true`：`max_stocks=500`, `L=20`, `seeds=[42]`, `hidden_size=64`——**架构不变**，仅缩小规模排错。

---

## 七、交易策略设计（对齐作业 + 实务增强）

### 7.1 基线策略（作业推荐）

- 参数 \(n,k\) 网格搜索（在**验证段**）：以 **ICIR、夏普、换手惩罚** 联合选参。
- **涨停/跌停过滤**：买入侧避开当日难以成交的涨停价附近；卖出侧对跌停流动性风险做规则约束。

### 7.2 扩展（仍保持可解释）

- **得分加权仓位**：在 top 组内按 softmax(score) 分配权重，而非严格等权。
- **风险约束**：行业最大暴露、单票最大权重、与沪深 300 的跟踪误差上限（需优化器，复杂度高于基线）。
- **与 T+1 一致**：每日可卖集合 = 昨日收盘持仓 ∩ 今日可卖；买入后锁仓至 \(+1\) 日。

### 7.3 与「满仓」约束

- 比赛要求尽可能满仓：策略层应显式把**现金比例**压到阈值以下，剩余买入预测分数次优股票直至资金用尽。

### 7.4 初学者导读：业界「从预测分数到持仓」怎么做，以及你们怎么对齐大作业 PDF

#### （1）量化股票策略的常见「翻译层」：预测 ≠ 组合

- **预测模型**输出的是每只股票的 **alpha 分数**（相对排序或预期超额收益）。  
- **组合构建（portfolio construction）**才决定：买多少只、各买多少权重、多久换一次、遇到涨停/停牌怎么办。  
- **业界经验**：很多团队 **IC 不错但 PnL 一般**，问题常出在翻译层（换手过高、冲击成本、约束没写）。

**你们实验里**：代码结构上建议分成三个函数：`model.predict(features) -> score`；`strategy.rebalance(score, holdings, cash) -> orders`；`backtest.simulate(orders) -> pnl`。

#### （2）作业 PDF 的 Top–Bottom \(k\)：一种极简「排序交易」规则

- **规则复述**：持有 \(n\) 只股票等权；每天卖掉分数最低的 \(k\) 只，买入全市场分数最高的 \(k\) 只（可与自己持仓重叠处理，实现时写清楚）。  
- **经济直觉**：相信模型在**横截面上**能分辨相对强弱；组合保持分散以控 idiosyncratic risk。  
- **与业界关系**：类似「**多空对冲的多头侧简化版**」或「高换手单多头 rotation」；完整对冲基金还会做空头与对冲 beta，你们作业不需要。

**你们实验里**：  
1. 先固定 `n=10, k=1` 跑通；再在**验证集**上网格 `n,k`（不要拿比赛周调参）。  
2. 记录每日换手、平均持有期，解释是否满足 PDF「调仓频率不应过高」。  
3. 实现 **T+1**：当日买入的仓位 `sellable_mask` 次日才为真，否则回测会「幽灵收益」。

#### （3）涨停/跌停、流动性：业界「可成交性过滤」

- **直觉**：模型说买某只，但若一字涨停，你挂限价也未必成交；回测若假设总能按收盘价成交，会高估收益。  
- **业界做法**：对不可买/不可卖状态做 **mask**；或用更保守的成交价假设（滑点）。

**你们实验里**：若不做逐笔撮合，至少在买入规则里加：`pct_chg < 9.5%`（主板近似）等过滤，并在报告写明这是**简化假设**。

#### （4）得分加权仓位：从「等权 Top-n」到「 softmax 权重」

- **文献/业界**：均值–方差框架下，最优权重常与预期收益近似线性，但需协方差矩阵估计（入门难度大）。** softmax(score)** 是轻量启发式：分数高的权重大，但仍分散。

**你们实验里**：作为扩展实验，对比「等权 Top-n」与「softmax 权重」在**验证夏普**与**最大回撤**上的差异即可。

#### （5）风险约束（行业上限、单票上限）：Barra 风险模型文化的「极简版」

- **业界**：用因子协方差矩阵控制组合对行业、规模、风格的暴露；你们可以用**硬约束**：单行业不超过 30%、单票不超过 10%。

**你们实验里**：在 `rebalance` 里加循环：若超配则把多余权重分配给次高分股票（与「满仓」逻辑一致）。

#### （6）满仓约束：比赛规则与业界「现金管理」不同

- **比赛**：要求尽可能满仓 → 现金视为「浪费机会」但会简化评测。  
- **业界**：常留部分现金应对赎回与保证金；这与作业不同，报告里一句话说明即可。

**你们实验里**：每日结束后若仍有现金，按分数从高到低继续买入直到买满或无可买标的，并在日志里记录「为何仍有现金」（例如全部涨停）。

#### （7）第七节自查：写报告时建议附两张图

1. **预测分数分位数**随时间是否漂移（模型老化）。  
2. **组合行业权重**随时间热力图（是否中性化失效）。

---

## 八、历史回测与评估体系

### 8.1 预测质量

- **IC**：每日截面相关系数序列的均值。
- **ICIR**：\(\text{mean(IC)}/\text{std(IC)}\)**，衡量稳定性**；高频 alpha 更看重 ICIR 而非单次高 IC。
- **方向胜率**（可选）：预测符号与实际符号一致的比例。

### 8.2 组合绩效

- **年化收益率、年化波动率、夏普比率、最大回撤**。
- **与基准对比**：沪深 300、上证综指、创业板指（数据已提供）。
- **成本敏感性**：双边千一至千三佣金 + 印花税（卖出）+ 5bps 滑点，检验是否「纸面 alpha」。

### 8.3 回测实施要点

- **前视偏差审计**：特征、 scaler、行业映射、指数成分均用 point-in-time 版本；新闻时间戳严格对齐。
- **幸存者偏差**：本数据集为当前上市列表历史，严格而言存在幸存者偏差；报告应**诚实讨论**（学术界常用 CRSP 风格完整库缓解，课程数据受限时需在结论中声明）。

### 8.4 文献与业界方法对照

- **Gu, Kelly & Xiu (2020), *Review of Financial Studies***：比较线性模型、树、神经网络等对大截面特征集的定价能力，发现**树与神经网络**占优，且重要信号与**动量、波动、流动性**相关。链接示例：  
  https://academic.oup.com/rfs/article/33/5/2223/5758276  
- **TFT**（Lim et al., 2021, *International Journal of Forecasting*）：多输入类型与可解释注意力；适合作为「时间序列模块」引用。  
  https://arxiv.org/abs/1912.09363  
- **101 Alphas**（Kakushadze, 2016）：因子工程算子思想。  
  https://arxiv.org/abs/1601.00991  
- **多模态/文本+价格**：可参考系统性综述类工作（如 arXiv:2212.12717 等对深度学习股价预测方法的分类综述；以及「Learning Across Modalities」类金融多模态综述，具体条目以 arXiv / SSRN 检索为准）。

### 8.5 初学者导读：IC、夏普、回撤在文献/业界里怎么用，以及你们实验「该算哪些、怎么解读」

#### （1）IC：为什么量化团队天天看「相关系数」？

- **定义直觉**：每个交易日，把模型预测的分数与**未来一期（或 \(n\) 期）收益**在几千只股票上做相关（常用 Spearman 秩相关）。再对时间求平均得到 **mean IC**。  
- **文献位置**：GKX 及大量实证资产定价论文用类似指标衡量「**横截面定价信息**」；它不要求你预测准绝对涨幅，只要求**排序对**。  
- **与深度学习损失的关系**：MSE 低不代表 IC 高；若你们发现「训练 loss 降了但 IC 不涨」，很常见，因为**噪声主导绝对误差**。

**你们实验里**：每天算一个 IC，画 **IC 时间序列折线图**；报告里同时给 **mean IC** 与 **ICIR**（均值/标准差）。ICIR 低表示「信号时有时无」，助教通常更信 ICIR。

#### （2）ICIR：「平均相关」不够，还要看「稳不稳」

- **直觉**：某策略一年里只有两个月 IC 极高、其余月份接近 0，mean IC 可能还行，但实盘很难依赖。ICIR 惩罚这种「不稳定」。  
- **业界用法**：高频 alpha 与多因子组合常同时报 IC 与 ICIR。

**你们实验里**：对比「中性化前后」「加新闻前后」两条 IC 序列，用 ICIR 总结哪条更稳。

#### （3）年化收益与夏普：组合层的「成绩」，但不等于预测层成绩

- **夏普比率（粗直觉）**：每承担一单位波动，平均多赚多少超额收益；分子常用「组合收益 − 无风险利率」，课程作业里无风险利率常近似为 0 或短债。  
- **文献提醒**：单一策略样本内夏普极高，要怀疑泄露或过拟合；Harvey 等关于「多重检验下阈值应更严」的讨论在业界很有名（初学者记住结论即可：**不要拿单一夏普吹嘘**）。

**你们实验里**：  
1. 回测输出：**年化收益、年化波动、夏普、最大回撤**；与 **沪深 300**（`market/000300.SH`） buy-and-hold 对齐同一时间轴对比。  
2. 做 **成本敏感性**：佣金+印花税+5bps 滑点各跑一遍，夏普是否崩塌——崩塌说明 alpha 很薄，这是诚实结论，报告里反而加分。

#### （4）最大回撤：「最坏路径」比「平均好坏」更决定你能不能拿住策略

- **直觉**：从历史峰值到谷底的最大跌幅；杠杆越高越怕回撤。  
- **业界**：很多产品合同与风控直接约束最大回撤。

**你们实验里**：在验证集回测曲线标注最大回撤区间（起止日期），并讨论是否发生在某一市场阶段（如 2015、2020 疫情等），与 Rossi（2021）「结构突变」叙事呼应。

#### （5）基准对比：不是形式主义，而是回答「我有没有赚到 beta」

- **直觉**：牛市里随便买股票也可能涨；你要证明模型相对 **市场宽基** 有 **超额（alpha）**。  
- **数据**：你们已有上证、沪深 300、创业板指日行情。

**你们实验里**：至少画两条净值曲线：**你的策略** vs **等权持有沪深 300**（或买入持有指数）；并报告 **信息比率（IR）** 近似：超额收益均值/超额收益波动（若课程未要求可省略公式，用文字描述）。

#### （6）前视偏差与「回测引擎」：业界与学术的共识

- **前视偏差例子**：用「全样本」算的行业分类回测过去；用未来才公布的指数成分选股；新闻时间晚于收盘却记入当日特征。  
- **业界**：回测系统强调 **point-in-time（PIT）数据库**；你们用 CSV 也要模仿这一精神。

**你们实验里**：写一页「**数据泄露审计清单**」：逐条对照第五节 5.11 表，并在附录给 1–2 个「故意做错版本」的对照（例如全样本 scaler）展示 IC 如何虚高——这是极强的教学展示（注意：对照实验勿与正式结果混淆）。

#### （7）幸存者偏差：你们数据能说什么、不能说什么

- **直觉**：若股票池只含「现在还活着的公司」，过去表现会被系统性抬高。  
- **学术**：严格研究需要包含退市股票；课程数据若做不到，要在报告 limitation 里写清楚。

**你们实验里**：结论里避免「我发现了 A 股永恒规律」式表述；改成「在提供的数据与股票池定义下，样本内/验证期表现……」。

#### （8）第八节「交作业级」输出清单（建议直接当报告目录小节）

1. 训练/验证 loss 曲线 + 早停说明。  
2. IC / ICIR 表 + IC 时间序列图。  
3. 回测：净值、年化、夏普、最大回撤 + 与沪深 300 对比。  
4. 成本敏感性表。  
5. 数据泄露与幸存者偏差讨论各一段。

---

## 九、业界实践归纳：为何这些方法有效

| 环节 | 常见做法 | 背后原理 |
|------|----------|----------|
| 标签 | 超额收益、多 horizon | 去除共同因子暴露后，预测**异质性风险调整收益**更稳定 |
| 截面处理 | rank、z-score、行业中性 | 降低分布漂移与非平稳性；与排序选股目标（IC）一致 |
| 模型 | 树集成 + 神经网络 | 高维交互与非平滑结构；正则化与早停防过拟合 |
| 文本 | 预训练语言模型 + 聚合 | 非结构化信息中的**事件冲击**与情绪扩散 |
| 评估 | IC/IR + 成本后回测 | 相关性与 PnL 不完全等价；成本检验可部署性 |
| 部署 | 滚动重训、监控漂移 | 概念漂移在制度与市场结构变化下不可避免 |

---

## 十、全数据使用的落地检查清单（建议写入正式实验报告）

1. **价量**：`daily` 全历史 + 指数 `market` 对齐。  
2. **基本面/资金流**：`metric`、`moneyflow` 与 `daily` 按 `ts_code, trade_date` 内连接。  
3. **新闻**：`news` 全量跑 NLP 管线；**2019 前**用 mask/仅子模型。  
4. **过滤**：`stock_st` 剔除 ST；`basic.market` 或 `ts_code` 剔除北交所。  
5. **无泄露 scaler 与标签 shift**。  
6. **联合模型**在验证集上报告 **各模态消融**（A/B/C 单独与融合）。  

---

## 十一、参考文献与链接（便于在终稿中按 BibTeX 规范化）

1. Shihao Gu, Bryan Kelly, Dacheng Xiu. *Empirical Asset Pricing via Machine Learning*. **The Review of Financial Studies**, 2020.  
   https://academic.oup.com/rfs/article/33/5/2223/5758276  
2. Stefano Giglio, Yuan Liao, Dacheng Xiu. *Thousands of Alpha Tests*. **The Review of Financial Studies**, 2021.  
   https://papers.ssrn.com/sol3/papers.cfm?abstract_id=3259268  
3. Guanhao Feng, Stefano Giglio, Dacheng Xiu. *Taming the Factor Zoo: A Test of New Factors*. **The Journal of Finance**, 2020.  
   https://doi.org/10.1111/jofi.12927  
4. Serhiy Kozak, Stefan Nagel, Shrihari Santosh. *Shrinking the Cross-Section*. **Journal of Financial Economics**, 2020.  
   https://doi.org/10.1016/j.jfineco.2019.07.008  
5. Erik Mekelburg, Jack Strauss. *Pooling and winsorizing machine learning forecasts to predict stock returns with high-dimensional data*. **Journal of Empirical Finance**, 2024, DOI: 10.1016/j.jempfin.2024.101538.  
   https://ideas.repec.org/a/eee/empfin/v79y2024ics0927539824000732.html  
6. Marcos López de Prado. *Advances in Financial Machine Learning*. Wiley, 2018.（Purged CV / embargo 等验证框架）  
7. Zura Kakushadze. *101 Formulaic Alphas*. arXiv:1601.00991, 2016.  
   https://arxiv.org/abs/1601.00991  
8. Bryan Lim et al. *Temporal Fusion Transformers for Interpretable Multi-Horizon Time Series Forecasting*. **International Journal of Forecasting**, 2021 (preprint arXiv:1912.09363).  
   https://arxiv.org/abs/1912.09363  
9. 金融情感与深度学习结合方向（示例预印本，终稿请核对卷期页）：  
   - arXiv:1906.09024；arXiv:2403.04427；arXiv:2407.16150 等。  
10. 深度学习股价预测综述类（示例）：arXiv:2212.12717（请在引用前核对题名与版本）。  
11. Tidy Finance 博客：GKX 复现中对截面秩变换至 [-1,1] 的实现说明：  
   https://www.tidy-finance.org/blog/gu-kelly-xiu-replication/  
12. Barbara Rossi. *Forecasting in the Presence of Instabilities*. **Journal of Economic Literature**, 2021（预测模型在结构变化下的评估与改进综述，可与 walk-forward 设计对照阅读）。

---

## 十二、小结

本作业在统计上是一个**大截面、短面板、强噪声**的排序预测问题；在工程上是一个**严格因果对齐**的数据管道问题。建议流程上**先完成第五节（预处理与特征工程）**：时间 walk-forward、purged/embargo 验证思想、逐期截面秩或 winsorize、行业与规模中性、量纲统一与防泄露标准化；再进入**第六节**多模态深度模型与联合打分；策略上实现 PDF 的 **Top–Bottom \(k\) 换手** 并满足 T+1 与满仓约束；评估上 **IC/IR 与成本后夏普、最大回撤** 并重，并与沪深 300 等基准对照。文献与业界上，该路线对齐 GKX（2020）的大截面机器学习资产定价实践、Giglio–Liao–Xiu（2021）与 Feng–Giglio–Xiu（2020）对数据窥探与因子冗余的警示、Kozak–Nagel–Santosh（2020）的收缩思想、Mekelburg–Strauss（2024）对预测集成与 winsorize 的实证支持，以及 101 Alphas 与 FinBERT 类工作的工程文化。

---

*报告撰写说明：PDF 内容已通过本地读取核对；`data` 具体字段以 `data/README.md` 为准；新闻与多股关联需在实现阶段补充算法细节与消融实验表格。*
