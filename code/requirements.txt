# TFNE-C A股深度学习项目依赖（Python >= 3.9）
# 在 conda 环境中：conda activate ai25 && python -m pip install -r requirements.txt

numpy>=1.24.0
pandas>=2.0.0
pyarrow>=14.0.0
scipy>=1.10.0
scikit-learn>=1.3.0
pyyaml>=6.0
tqdm>=4.66.0
matplotlib>=3.7.0
seaborn>=0.13.0

# 若已用 conda 安装 pytorch，可跳过下一行
torch>=2.0.0

# 注意：pypi 上的 rtdl 要求 torch<2，与 PyTorch 2.x 冲突。
# 截面模型会自动回退为 MLP（见 src/models/tab_model.py），无需安装 rtdl。

# 新闻 LoRA（可选；config 中 use_news_bert: false 时可不装）
# 单独安装：python -m pip install -r requirements-nlp.txt