# TFNE-C2 · 第二代模型设想与服务器提交指南

> **代号**：TFNE-C2（Temporal Fusion + FT-Transformer + NewsLoRA，第二代）  
> **配置**：`code/config.server_c2_72h.yaml`  
> **产物目录**：`code/artifacts/**/tier_c2/`（**不覆盖** job 7707 的 `tier_c`）  
> **入口**：`code/scripts/run_server_c2_72h.py`

---

## 一、为什么要做 C2？

第一代（job **7707**，`config.server_c_tfne_72h.yaml`）在 RTX 5090 上 **约 3.2 h** 就跑完了 train+eval，说明 **72 h GPU 配额严重未用尽**。同时暴露出以下问题：

| 类别 | 7707 失误 / 局限 | C2 对策 |
|------|------------------|---------|
| **规模** | Top2000、1 seed、max_epochs=8 | Top**3000**、**3 seed**、max_epochs=**18** |
| **数值** | TFT 开 AMP/TF32 曾 loss=nan | 保持 **amp=false, tf32=false** + dataset sanitize |
| **News** | 训练子采样 4 万；04 不自动从 checkpoint 推理新日期 | 子采样 **8 万**；**04 自动 News checkpoint 推理** |
| **切分** | train 止于 2024-06，valid 为 2024H2 | train 至 **2025-03-31**，valid=**2025-04~数据末**（赛前 holdout） |
| **模拟赛** | 融合权重基于 2024H2，与 2026 赛前行情脱节 | valid 改为 **2025Q2–2026-05** 定 ICIR 权重 |
| **严谨性** | 为抢 72h 过度缩减，难以写「满血 C」 | 在 **实测耗时反推** 的预算表内尽量拉满 |
| **产物** | 与 smoke/A 共用 artifacts 根目录易混淆 | 新增 **`artifacts_tag: tier_c2`** |

C2 的目标：**在单次 GPU 72 h 内，把能加的规模都加上**，同时保持时间因果切分、防泄露流程不变，并优化模拟赛 forward 预测链路。

---

## 二、科学严谨性：切分与评估怎么设计？

### 2.1 时间切分（正序，禁止「近期训、早期测」）

```
2019-01 ───────────── train ───────────── 2025-03-31
                    │ valid（赛前 holdout）│
              2025-04-01 ─────────────── 2026-05-20
                    │ test（若有更新数据）  │
              valid_end 的次日 ───────────→ 未来
```

| 集合 | 区间 | 用途 |
|------|------|------|
| **train** | ≤ 20250331 | TFT / FTT / NewsLoRA 训练 |
| **valid** | 20250401 – 20260520 | 早停、Spearman IC、**ICIR 融合定权**、valid 回测 |
| **test** | > valid_end | 仅当 prep 后还有更新日期时出现；模拟赛 forward 段 |

**说明（写报告时用）：**

- 7707 的独立 test（2025–2026）在 C2 中 **大部分进入 train/valid**，因此 **不宜直接数值对比** 7707 test 回测 27% 与 C2 valid 回测。
- C2 的 valid 段是 **「赛前样本外 holdout」**：模型训练未见过 2025-04 之后的数据，用于定权与策略评估，符合因果原则。
- 模拟赛（2026-06 起）属于 **forward predict**：用 frozen checkpoint + 更新 CSV + `04`/`07`，不再用未来标签。

### 2.2 防泄露（继承第一代修复）

- 特征：逐日截面 winsorize + rank（`src/preprocess.py`）
- 标签：`excess_ret_1d` 为 T+1，特征仅用 ≤T 信息
- 新闻：T+1 映射（`src/news_link.py`）
- 静态特征 NaN：`listing_age` / `log_mv` 已在 `src/dataset.py` sanitize（7707 教训）
- NaN 列：预检 **WARN 不阻断**（`src/preflight.py`）

### 2.3 模拟赛表现：策略层不变，模型层加强

策略参数与 7707 相同（Top10% 选股、最多 50 只、T+1、成本参数见 config `strategy` 段），便于隔离「模型升级」效果。

模拟赛每日流程：

1. 收盘后更新 `data/daily/`、`data/news/` 等  
2. `prep`（若尚未跑过可跳过）或仅 `04` + `07`  
3. 读 `artifacts/orders/tier_c2/orders_YYYYMMDD.csv` 下单  

C2 新增 **`ensemble.predict_news_from_checkpoint: true`**，更新 CSV 后 **不必重训 News** 即可刷新融合 score。

---

## 三、72 h GPU 预算：基于 7707 实测的精确外推

### 3.1 7707 实测（RTX 5090，Top2000，1 seed）

| 阶段 | 耗时 |
|------|------|
| TFT（5 epoch 早停） | **1.35 h** |
| FTT（8 epoch） | **1.74 h** |
| NewsLoRA | **0.15 h** |
| eval（04–07） | **~0.5 h** |
| **合计** | **~3.24 h** |

推导单 epoch 耗时（@Top2000）：

- TFT：1.35 / 5 ≈ **16.2 min/epoch**
- FTT：1.74 / 8 ≈ **13.1 min/epoch**

### 3.2 规模缩放系数

Top2000 → Top3000：面板行数约 ×**1.45**（356万→约517万行量级），单 epoch 耗时近似 ×**1.45**。

@Top3000 单 epoch 粗算：

- TFT：**~23.5 min/epoch**
- FTT：**~19.0 min/epoch**
- News（8 万样本 vs 4 万）：**~0.35 h/seed**（含全量 predict）

### 3.3 C2 全流水线预算（3 seed）

| 模块 | 假设 | 耗时估算 |
|------|------|----------|
| TFT ×3 | 早停均 ~10 epoch | 3 × 10 × 23.5min ≈ **11.8 h** |
| FTT ×3 | 早停均 ~12 epoch | 3 × 12 × 19min ≈ **11.4 h** |
| News ×3 | 4 epoch + predict | ≈ **1.1 h** |
| eval ×3 seed 融合 | 04 全量推理 Top3000 | **2–4 h** |
| **典型合计** | | **~27–30 h** |
| **极端**（跑满 18 epoch） | TFT+FTT 均少早停 | **~50–58 h** |
| **72 h 余量** | | **≥14 h** |

结论：C2 在 **实测基线 3.2 h → 预算 ~30 h** 的框架下，已是 **72 h 内接近满血** 的配置；若极端情况接近 58 h，仍在配额内。

> CPU prep（Top3000）单独 **48 h sbatch**，不占 GPU 72 h。

---

## 四、C2 相对 7707 的配置对照

| 项目 | 7707 (tfne_72h) | **C2 (c2_72h)** |
|------|-----------------|-----------------|
| artifacts | `tier_c` | **`tier_c2`** |
| 股票池 | Top 2000 | **Top 3000** |
| seeds | [42] | **[42, 123, 456]** |
| max_epochs | 8 | **18** |
| patience | 3 | **6** |
| News 训练样本 | 40k | **80k** |
| train_end | 20240630 | **20250331** |
| valid | 2024H2 | **20250401–20260520** |
| News 增量 predict | 需重训 03 | **04 自动 checkpoint 推理** |
| 7707 实测 GPU | 3.2 h | C2 估算 **28–42 h** |

架构不变：**TFT-lite + FT-Transformer + NewsLoRA**，秩融合 + 目标权重策略。

---

## 五、代码改动摘要

| 文件 | 改动 |
|------|------|
| `config.server_c2_72h.yaml` | C2 超参与切分、耗时备注 |
| `scripts/run_server_c2_72h.py` | prep / train / eval / predict-news |
| `scripts/slurm_prep_cpu_c2_72h.sbatch` | CPU prep 作业 |
| `scripts/slurm_train_gpu_c2_72h.sbatch` | GPU 72h train+eval |
| `src/config.py` | `artifacts_tag`、`valid_start()` |
| `src/news_predict.py` | News 全面板推理（新） |
| `scripts/03_predict_news_lora.py` | 仅推理、不重训（新） |
| `scripts/04_ensemble_predict.py` | 自动 News checkpoint；valid_start 读 config |
| `scripts/06_backtest.py` | valid/test 区间读 config |
| `src/preflight.py` | 识别 `tfne_c2_72h_balanced` profile |

---

## 六、服务器提交步骤（逐步复制）

### 6.1 路径与环境

| | 路径 |
|---|------|
| 本机项目 | `C:\Users\Lenovo\Desktop\deep learning\pro` |
| 服务器 home | `/home/scc/pb23151824/` |
| 代码 | `/home/scc/pb23151824/pro/code` |
| 数据 | `/home/scc/pb23151824/pro/data` |
| conda | `ai25` |

### 6.2 本机：打包最新 code

```powershell
cd "C:\Users\Lenovo\Desktop\deep learning\pro"
tar -czvf "C:\Users\Lenovo\Desktop\deep learning\pro\code.tar.gz" code
```

打包前确认存在：

```text
code/config.server_c2_72h.yaml
code/scripts/run_server_c2_72h.py
code/scripts/slurm_prep_cpu_c2_72h.sbatch
code/scripts/slurm_train_gpu_c2_72h.sbatch
code/src/news_predict.py
```

`data.tar.gz` 若服务器已有且未更新 CSV，**不必重传**。

### 6.3 服务器：上传并解压

1. 网页文件管理上传 `code.tar.gz` 到 `/home/scc/pb23151824/`
2. Shell 执行：

```bash
cd /home/scc/pb23151824
rm -rf pro/code
tar -xzf code.tar.gz -C pro
ls pro/code/config.server_c2_72h.yaml
ls pro/code/scripts/run_server_c2_72h.py
```

### 6.4 清理旧 prep（C2 必须 Top3000 重跑 prep）

C2 与 7707（Top2000）**不能共用** `artifacts/`：

```bash
rm -rf /home/scc/pb23151824/pro/code/artifacts
```

> 7707 的 `tier_c` 结果若需保留，请在删之前从服务器 `scp` 备份 `artifacts/metrics/tier_c` 等目录。

### 6.4.5 ⚠️ 秒失败 / 无 err（SCOW 平台专用）

作业 **1 秒 FAILED** 且 **没有 `.err`** = Slurm **还没执行 bash** 就失败了。按 USTC 官方 prep 文档（`code/docs/USTC_SLURM_PREP.md`），常见原因如下：

| # | 错误写法 | 正确写法 |
|---|----------|----------|
| 1 | 标准输出 `/logs/prep_c2_%j.out` | **`logs/prep_c2_%j.out`**（相对路径，**不要**开头 `/`） |
| 2 | 标准输出 `/home/.../logs/...` 绝对路径 | 同上，用 **`logs/prep_c2_%j.out`** |
| 3 | 工作目录 `scow/jobs/training-job-...` | **`/home/scc/pb23151824/pro/code`** |
| 4 | prep 时 GPU = **1** | prep 必须 **0** |
| 5 | 命令框里写 `#SBATCH --output=...` | **禁止**；日志**只**在网页表单填 |
| 6 | 未提前创建 `logs` 目录 | 提交前 Shell 执行 **`mkdir -p /home/scc/pb23151824/pro/code/logs`** |

**日志在哪看：** 成功后文件在  
`/home/scc/pb23151824/pro/code/logs/prep_c2_<作业号>.out`  
或在作业详情页点 **「标准输出」**。

---

### 6.4.6 先提交 10 秒测试作业（强烈建议）

确认表单能跑通再提交 prep。表单与 prep **完全相同**，仅改作业名和命令：

| 字段 | 值 |
|------|-----|
| 作业名 | `c2-test` |
| 工作目录 | `/home/scc/pb23151824/pro/code` |
| 标准输出 | **`logs/test_%j.out`** |
| 错误输出 | **`logs/test_%j.err`** |
| GPU | **0** |

**命令框（仅 3 行）：**

```bash
echo "TEST OK $(date -Iseconds)"
pwd
ls /home/scc/pb23151824/pro/code/config.server_c2_72h.yaml
```

- 若 **c2-test 也秒失败** → 仍是表单路径/GPU 问题，不要提交 prep。  
- 若 **c2-test 成功** → 按 6.5 提交正式 prep。

---

### 6.5 第一步：网页提交 CPU prep（48 h）

打开平台 **「提交作业」** 页面。**先对照 6.4.5**，再逐项填写。

#### 6.5.0 表单里三个关键字段（复制粘贴）

提交前 Shell 执行：

```bash
mkdir -p /home/scc/pb23151824/pro/code/logs
```

| 字段 | 填写值（原样复制） |
|------|-------------------|
| **工作目录** | `/home/scc/pb23151824/pro/code` |
| **标准输出文件** | **`logs/prep_c2_%j.out`** |
| **错误输出文件** | **`logs/prep_c2_%j.err`** |

> 必须是 **`logs/...` 相对路径**，不要 `/logs/...`，不要 `/home/.../logs/...`。

#### 6.5.1 表单字段（prep · 无 GPU）

| 字段 | 填写值 |
|------|--------|
| **集群** | `本科生算力平台` |
| **作业名** | `c2-prep` |
| **账户** | `stu` |
| **分区** | `Students` |
| **QOS** | `qos_stu_long` |
| **节点数** | `1` |
| **单节点加速卡卡数** | **`0`** |
| **最长运行时间** | `4320` 分钟，或 `72` 小时 / `3` 天 |
| **工作目录** | `/home/scc/pb23151824/pro/code` |
| **标准输出文件** | **`logs/prep_c2_%j.out`** |
| **错误输出文件** | **`logs/prep_c2_%j.err`** |

底部资源条：**总加速卡 = 0**。

#### 6.5.2 「命令」框（整段粘贴）

**不要**在命令里写 `#SBATCH --output` / `#SBATCH --error`（会报 Invalid directive 并秒失败）。  
完整脚本也可从 `code/scripts/web_c2_prep_command.sh` 复制。

```bash
#!/bin/bash
#SBATCH --job-name=c2-prep
#SBATCH --partition=Students
#SBATCH --qos=qos_stu_long
#SBATCH --nodes=1
#SBATCH --gres=gpu:0
#SBATCH --cpus-per-task=16
#SBATCH --mem=64G
#SBATCH --time=72:00:00

set -euo pipefail
echo "========== C2 prep START $(date -Iseconds) host=$(hostname) =========="

CODE_DIR="/home/scc/pb23151824/pro/code"
DATA_DIR="/home/scc/pb23151824/pro/data"

mkdir -p "${CODE_DIR}/logs"
cd "${CODE_DIR}"

set +u
if [ -f /home/scc/pb23151824/miniforge3/etc/profile.d/conda.sh ]; then
  source /home/scc/pb23151824/miniforge3/etc/profile.d/conda.sh
elif [ -f /home/scc/pb23151824/anaconda3/etc/profile.d/conda.sh ]; then
  source /home/scc/pb23151824/anaconda3/etc/profile.d/conda.sh
else
  echo "ERROR: 未找到 conda"; exit 1
fi
conda activate ai25
set -u

test -d "${DATA_DIR}" || { echo "ERROR: 未找到 ${DATA_DIR}"; exit 1; }
test -f "${CODE_DIR}/config.server_c2_72h.yaml" || { echo "ERROR: 缺少 config.server_c2_72h.yaml"; exit 1; }

pip install -r "${CODE_DIR}/requirements.txt" -q
python "${CODE_DIR}/scripts/run_server_c2_72h.py" prep

ls -lh "${CODE_DIR}/artifacts/panel_daily.parquet"
ls -lh "${CODE_DIR}/artifacts/features/panel_features.parquet"
ls -lh "${CODE_DIR}/artifacts/news/panel_news.parquet"
echo "[CHECK] prep OK $(date -Iseconds)"
```

填完后点 **「提交」**。

#### 6.5.3 如何看 prep 是否成功

1. 打开 **「作业管理」**，找到 `c2-prep`，等状态变为 **已完成**。  
2. 查看标准输出，或打开文件：  
   `/home/scc/pb23151824/pro/code/logs/prep_c2_<作业号>.out`  
3. **成功标志**（末尾）：

```text
[CHECK] prep OK
20240701 股票数: ... （应约 3000）
```

---

### 6.6 第二步：网页提交 GPU train + eval（72 h）

**仅在 6.5 prep 成功后再提交。**

#### 6.6.1 表单字段（train+eval · 1 块 GPU）

| 字段 | 填写值 |
|------|--------|
| **集群** | `本科生算力平台` |
| **作业名** | `c2-gpu` |
| **账户** | `stu` |
| **分区** | `Students` |
| **QOS** | `qos_stu_long` 或 `qos_stu001` |
| **节点数** | `1` |
| **单节点加速卡卡数** | **`1`** |
| **最长运行时间** | **`4320`** 分钟，或 **`72`** 小时（二选一，都是 72h） |
| **工作目录** | `/home/scc/pb23151824/pro/code` |
| **标准输出文件** | **`logs/train_c2_%j.out`** |
| **错误输出文件** | **`logs/train_c2_%j.err`** |
| **备注** | 可填 `TFNE-C2 train+eval 3seed Top3000` |

底部资源条应约为：**总节点数 1、总加速卡 1、总 CPU 核心 16、总内存约 72 GB**。

> ⚠️ **必须填 4320 分钟**，不能填 30 或 480。

#### 6.6.2 「命令」框（整段粘贴）

```bash
#!/bin/bash
#SBATCH --job-name=c2-gpu
#SBATCH --partition=Students
#SBATCH --qos=qos_stu_long
#SBATCH --nodes=1
#SBATCH --gres=gpu:1
#SBATCH --cpus-per-task=16
#SBATCH --mem=64G
#SBATCH --time=72:00:00

set -euo pipefail
echo "========== C2 GPU START $(date -Iseconds) =========="

CODE_DIR="/home/scc/pb23151824/pro/code"
FEAT="${CODE_DIR}/artifacts/features/panel_features.parquet"

mkdir -p "${CODE_DIR}/logs"
cd "${CODE_DIR}"

set +u
source /home/scc/pb23151824/miniforge3/etc/profile.d/conda.sh
conda activate ai25
set -u

export HF_ENDPOINT=https://hf-mirror.com
export DISABLE_SAFETENSORS_CONVERSION=true
pip install -r "${CODE_DIR}/requirements-nlp.txt" -q

test -f "${FEAT}" || { echo "ERROR: 先完成 prep"; exit 1; }

python "${CODE_DIR}/scripts/check_gpu.py"
python "${CODE_DIR}/scripts/run_server_c2_72h.py" train
python "${CODE_DIR}/scripts/run_server_c2_72h.py" eval

echo "[CHECK] train+eval OK $(date -Iseconds)"
```

填完后点 **「提交」**。

#### 6.6.3 如何看 train 是否成功

1. **作业管理** → `c2-gpu` → 输出文件：  
   `/home/scc/pb23151824/pro/code/logs/train_c2_<作业号>.out`  
2. 训练中应出现（不能是 `nan`）：

```text
epoch 1/18: loss=0.xxxx val_ic=0.0x
```

3. **成功标志**（末尾）：

```text
[CHECK] train+eval OK
```

---

### 6.7 产物检查（文件管理或 Shell）

| 检查项 | 路径 |
|--------|------|
| TFT 权重 | `artifacts/checkpoints/tier_c2/tft/seed_42/best.pt` |
| FTT 权重 | `artifacts/checkpoints/tier_c2/ftt/seed_456/best.pt` |
| 融合 score | `artifacts/predictions/tier_c2/ensemble_scores.parquet` |
| 最新订单 | `artifacts/orders/tier_c2/orders_*.csv` |
| IC 汇总 | `artifacts/metrics/tier_c2/ic_summary.json` |

路径均在 `/home/scc/pb23151824/pro/code/` 下。

---

### 6.8 训练中断：网页续跑（可选第三次作业）

表单与 **6.6 相同**，仅改作业名与日志路径；**单节点加速卡卡数仍为 1**，**最长运行时间建议 4320 分钟**。

| 字段 | 填写值 |
|------|--------|
| **作业名** | `c2-gpu-resume` |
| **标准输出** | `/home/scc/pb23151824/pro/code/logs/resume_c2_%j.out` |
| **错误输出** | `/home/scc/pb23151824/pro/code/logs/resume_c2_%j.err` |

**「命令」框**（例：只补 seed 123 的 FTT，再 eval）：

```bash
#!/bin/bash
set -euo pipefail
CODE_DIR="/home/scc/pb23151824/pro/code"
CONDA_SH="/home/scc/pb23151824/miniforge3/etc/profile.d/conda.sh"
cd "${CODE_DIR}"
mkdir -p logs
set +u
source "${CONDA_SH}"
conda activate ai25
set -u
export HF_ENDPOINT=https://hf-mirror.com
python "${CODE_DIR}/scripts/run_server_c2_72h.py" train-ftt --seeds 123
python "${CODE_DIR}/scripts/run_server_c2_72h.py" eval
echo "[CHECK] resume OK $(date -Iseconds)"
```

若训练已全部完成、只需出报告/订单，命令改为一行：

```bash
python "${CODE_DIR}/scripts/run_server_c2_72h.py" eval
```

### 6.9 模拟赛：每日更新数据后 predict（不重训）

```bash
cd /home/scc/pb23151824/pro/code
conda activate ai25

# 若 CSV 有新日期，先 prep（或本机跑）
python scripts/run_server_c2_72h.py prep

# 仅刷新 score + 订单（GPU 推荐）
python scripts/run_server_c2_72h.py eval
# 或拆步：
python scripts/04_ensemble_predict.py --config config.server_c2_72h.yaml
python scripts/07_predict_latest.py --config config.server_c2_72h.yaml
```

---

## 七、操作检查清单

- [ ] 本机 `tar` 打包含 C2 配置与脚本的新 `code.tar.gz`
- [ ] 服务器 `rm -rf pro/code` 后解压（**勿覆盖 7707 产物 unless 已备份**）
- [ ] `rm -rf pro/code/artifacts`（Top3000 必须新 prep）
- [ ] 网页提交 **prep** 作业（6.5）：GPU=**0**，时长=**2880 分钟** → 日志 `[CHECK] prep OK`，股票数 ≈3000
- [ ] 网页提交 **train+eval** 作业（6.6）：GPU=**1**，时长=**4320 分钟** → 日志 `[CHECK] train+eval OK`
- [ ] 报告写 **tier_c2** 指标；与 7707 **tier_c** 对比时需说明切分不同
- [ ] 模拟赛用 `config.server_c2_72h.yaml` + `orders/tier_c2/`

---

## 八、风险与预期管理

1. **valid 回测可能仍偏弱**：7707 已证明 IC≈0.07 时组合层可接近 0 收益；C2 加强的是 **排序与融合稳定性**，不保证组合一定盈利。  
2. **test 段可能为空**：valid_end=20260520 与数据末重合时，无独立 test；以 valid + forward 模拟赛为准。  
3. **OOM**：若 Top3000 batch4096 OOM，config 中改 `batch_stocks: 3072`。  
4. **loss=nan**：确认 `amp: false` 且 prep 为 Top3000 新产物。  

---

## 九、相关命令速查

```bash
# 预检（GPU 节点）
python scripts/validate_tier_c_readiness.py --config config.server_c2_72h.yaml
python scripts/check_panel_features_health.py --config config.server_c2_72h.yaml

# 本地/服务器手动全流程
python scripts/run_server_c2_72h.py prep
python scripts/run_server_c2_72h.py train
python scripts/run_server_c2_72h.py eval
```

**第一代对照**：7707 / `config.server_c_tfne_72h.yaml` / `tier_c`  
**第二代（本方案）**：C2 / `config.server_c2_72h.yaml` / `tier_c2`
