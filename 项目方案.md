# A 股深度学习趋势预测与模拟交易——完整项目方案

> **版本**：v2.0（**暂定档位 C**，与 `讨论报告.md`、`深度学习基础大作业.pdf`、`大作业评分细则.md` 及 `data/README.md` 对齐）  
> **定位**：在 **云算力 long 方案（16 核 CPU、1 GPU、单次最长 72h）** 上完成主实验；本地仅做小子集调试。强调 **无数据泄露、多模态深度学习、多 seed 集成、可复现**。  
> **方案名称**：**TFT + FT-Transformer + NewsLoRA 多 seed 秩融合集成（TFNE-C，档位 C）**  
> **三档运行**：档位 A 基线、档位 C 本地 smoke、档位 C 主实验的入口与产物目录见 **第十五节**。

---

## 一、任务与数据约束（必读）

### 1.1 作业任务（PDF 摘要）

| 环节 | 要求 |
|------|------|
| 预测 | 用过去 \(L\) 日特征序列，预测**未来短期收益方向**（实现为 \(n\) 日收益或其排序） |
| 模型 | **必须含神经网络**；允许多模型 |
| 交易 | **目标权重组合**（score→权重→调仓）；A 股 **T+1**；流动性/行业约束；详见第八节 |
| 股票池 | 除 **ST**、**北交所** 外全部 A 股（约 5000 只）；开发期可子集，**正式结果用全池或 Top3000 流动性** |
| 评估 | 训练/验证 **loss**；**IC、ICIR**；历史回测 **年化收益、夏普、最大回撤**；对比指数 |
| 红线 | **禁止未来信息**；禁止全样本随机打乱；禁止全历史 fit 标准化 |

### 1.2 评分细则对齐

| 评分项 | 档位 C 本方案对应 |
|--------|-------------------|
| 合理性（10%） | GKX 式截面预处理 + TFT/FT-Transformer 与任务匹配 + walk-forward |
| 创新性（15%） | 三模态深度模型 + **LoRA 新闻微调** + **多 seed 秩融合** + 文献可引用（TFT、GKX、Mekelburg 2024） |
| 模型效果（20%） | IC/ICIR + 回测；相对 **档位 A 小模型** 与 **单模态消融** 的提升 |
| 报告（25%） | 训练曲线、变量重要性（TFT）、消融表、泄露审计 |
| 规则依从（10%） | T+1、ST/北交所过滤、无泄露预处理、标签与执行时点一致 |
| 可复现（5%） | `config.yaml`、分阶段脚本、`requirements.txt`、固定多 seed |

### 1.3 数据资产核对（已读样例）

| 数据 | 存储形式 | 样例规模/注意 |
|------|----------|----------------|
| `basic.csv` | 单表 | ~5517 行 |
| `trade_cal.csv` | 单表 | `is_open` 0/1 |
| `daily/YYYYMMDD.csv` | 按日截面 | 单日 ~5300 股；`amount` **千元** |
| `metric/YYYYMMDD.csv` | 按日截面 | `pe` 亏损为空 |
| `moneyflow/YYYYMMDD.csv` | 按日截面 | `*_amount` **万元** |
| `news/YYYYMMDD.csv` | 按自然日 | 2019 起；需映射 `trade_date` |
| `stock_st/` | 按日 | ST 过滤 |
| `market/*.csv` | 指数全历史 | `000300.SH` 等 |
| `index_weight/` | 月度 | 仅评估用 |

---

## 二、方案总览：档位 C 流水线

```
原始 CSV
  → [1] 面板构建（16 核 CPU 并行，Parquet）
  → [2] 特征工程（L=60，~20 维时序 + 截面 + 新闻链指）
  → [3] 标签 excess_ret_1d，因果对齐
  → [4] 逐日截面 winsorize + 秩→[-1,1]（GKX）
  → [5] 子模型 A：TFT（时序） | B：FT-Transformer（截面） | C：NewsLoRA（文本）
  → [6] 多 seed 训练 → 预测 winsorize → 秩融合 + 可选门控（mkt_vol）
  → [7] score 截面 winsorize + 秩融合（Mekelburg）→ 目标权重组合（第八节）
  → [8] IC/ICIR（预测层）+ 组合回测（策略层）+ 沪深300 基准（第九节）
```

**设计原则**：**第五节预处理 → 第六节特征 → 第七节建模 → 第八节策略 → 第九节评估** 共用同一面板、同一时间切分、同一标签 `excess_ret_1d`；策略只消费 `artifacts/predictions/` 中的 `score`，不引入额外未来信息。

---

## 三、选用文献与业界方法（档位 C）

| 模块 | 选用方法 | 主要依据 |
|------|----------|----------|
| 预处理 | 逐日截面 winsorize + 秩→[-1,1]；walk-forward | **Gu, Kelly & Xiu (2020, RFS)** |
| 验证 | 时间切分；\(n=1\) 不用随机 K 折 | **López de Prado (2018, AFML)** |
| 特征 | 扩展动量/波动/流动性 + 101 Alphas 风格算子 | **Kakushadze (2016)**；GKX |
| 时序建模 | **Temporal Fusion Transformer (TFT)** | **Lim et al. (2021, IJF)** arXiv:1912.09363 |
| 截面建模 | **FT-Transformer**（`d_model=128`, `n_layers=4`） | Gorishniy et al.; GKX 表格深度精神 |
| 文本建模 | **RoBERTa-wwm + LoRA**（rank=8）+ 注意力池化 | FinBERT 类；arXiv:2306.02136 等 |
| 集成 | **3–5 seed** + 预测截面 winsorize + **秩融合** | **Mekelburg & Strauss (2024, JEF)** |
| 策略/评估 | 目标权重组合 + IC/ICIR + 成本后回测 | 见第八节（主方案不拘泥 PDF Top-k） |

**文献选用说明（为何主依据不追 2024–2025 最新大模型）**：表中较早文献（GKX、AFML、101 Alphas）锚定的是**无泄露预处理、时间切分验证与截面 IC**，与作业 PDF 同构；2021–2024 文献则对应**可开源复现**的 TFT、FT-Transformer、LoRA 与 Mekelburg 式集成。FinLLM、多模态大模型等 2023–2025 方向在**第七节 7.6**作对比讨论、**不替代**主实验，因其数据链指与样本外 IC/回测设定往往与本作业不一致，且闭源大模型不利于课程可复现与审计；更广调研见 `讨论报告.md`。

**档位 A 定位**：小 GRU+MLP+冻结 BERT，用于 **本地 1 日内排错**，不作为正式主结果。

---

## 四、数据选用与字段映射（不变，满足附加数据覆盖）

与 v1.0 相同，简述：

- **basic**：`ts_code`, `industry`, `market`, `list_date`（过滤北交所/ST）  
- **daily**：`close`, `pct_chg`, `vol`, `amount`, `vwap`, `high`, `low`, `pre_close`  
- **metric**：`turnover_rate_f`, `volume_ratio`, `pb`, `pe_ttm`, `pe_missing`, `log_mv`  
- **moneyflow**：`net_mf_amount` → `net_mf_ratio`；`elg_imb`, `lg_imb`  
- **news**：`datetime`, `title`, `content` → 链指 + LoRA 编码  
- **market**：`pct_chg` → `mkt_ret`, `mkt_vol`；标签 `excess_ret_1d`  
- **index_weight**：**仅评估**（沪深300 超额、成分内 IC 稳健性）  
- **stock_st / trade_cal**：过滤与对齐  

未使用字段：`cnspell`, `act_name`, `ps/ps_ttm/dv_*`, 资金流 16 列原始买卖量额等。

---

## 五、数据预处理（与档位 A 相同，严格执行）

### 5.1 面板构建（`scripts/01_build_panel.py`）

- **输出**：`artifacts/panel_daily.parquet`（建议按 `trade_date` 分区）。  
- **CPU**：`multiprocessing` / `joblib` 并行读日 CSV（发挥 **16 核**）。  
- **区间**：`2019-01-01` 起（新闻可用）。  
- **新闻时间**：`datetime` > 当日 15:00 → `news_trade_date = next_trading_day`；否则映射到当日交易日。  
- **全池**：正式训练 **不抽样**；本地调试可 `max_stocks=800` 或 `liquidity_top_k=1500`。

### 5.2 标签

```text
fwd_ret_1d(t)      = close(t+1)/close(t) - 1
excess_ret_1d(t)   = fwd_ret_1d(t) - mkt_ret(t+1)
```

**训练目标**：`excess_ret_1d`（Huber 或 MSE）。**IC** 用 `score(t)` vs `excess_ret_1d(t)`。  
**与策略对齐**：执行时点为 \(t\) 信号 → \(t+1\) 成交，与标签一致；可投资池流动性门槛与 **8.2** 共用（建议在 `01_build_panel` 中预计算 `amount_ma20`）。

### 5.3–5.4 缺失值、逐日截面 winsorize + 秩→[-1,1]

与 v1.0 相同；**禁止** `StandardScaler.fit(全历史)`。

### 5.5 时间切分

| 集合 | 区间 | 用途 |
|------|------|------|
| 训练 | 2019-01-01 — 2024-06-30 | 拟合 |
| 验证 | 2024-07-01 — 2024-12-31 | 早停、融合权重、策略超参（\(N_{\max},\tau,\delta\) 等）、门控 |
| 测试 | 2025-01-01 — 2025-12-31 | **一次** OOS，不调参 |
| 模拟赛 | 最新至 T-1 | `06_predict_latest.py` |

---

## 六、特征工程（档位 C 增强）

### 6.1 时序分支（TFT）— 窗口 **L=60**

在 `ts_code` 内滚动（仅用过去），约 **18–22 维**：

| 类别 | 特征（示例） |
|------|----------------|
| 收益 | `ret_1d`, `ret_5d`, `ret_10d`, `ret_20d`, `ret_60d` |
| 波动 | `vol_10d`, `vol_20d`, `vol_60d` |
| 价量 | `amp`, `vwap_dev`, `log_amount`, `turnover_rate_f`（滞后） |
| 资金流 | `net_mf_ratio`, `elg_imb`, `lg_imb` |
| 技术 | `rsi_14`, `macd_hist`（可选，仍因果） |

→ **`X_seq ∈ R^{60 × d_seq}`**，\(d_seq \approx 20\)。

**静态协变量（TFT 专用）**：`industry_id`, `log_mv`（当日）, `listing_age_days`（由 `list_date` 与 `t` 算，只用过去）。

**已知未来输入（可选，默认关闭）**：不使用「未来宏观路径」；仅 **`mkt_vol` 历史序列** 作为 **已观测到的市场状态通道**（截至 \(t\)），避免泄露争议。

### 6.2 截面分支（FT-Transformer）— 当日

| 特征 | 来源 |
|------|------|
| `turnover_rate_f`, `volume_ratio`, `pb`, `pe_ttm`, `pe_missing`, `log_mv` | metric（已秩变换） |
| `net_mf_ratio`, `elg_imb`, `lg_imb` | moneyflow |
| `industry_id` | basic → Embedding |

### 6.3 新闻分支（NewsLoRA）

- 链指：`basic.name` 子串 + 可选 `symbol`；无法链指 → **市场层** `market_news_score`。  
- 骨干：`hfl/chinese-roberta-wwm-ext` + **PEFT LoRA**（`r=8`, `alpha=16`, target `query`,`value`）。  
- 每条新闻 → 768 维 → **多头注意力池化**（可学习 query）→ `news_vec ∈ R^{64}` → Linear → `z^C`。  
- **预计算**：`02_embed_news.py` 可先缓存 tokenized 批次；微调阶段 **GPU 24–36h 内分段完成**。  
- 2019 前：`has_news=0`，融合门控自动降权。

### 6.4 融合门控输入

`[z^A, z^B, z^C, mkt_vol, has_news]` → 小 MLP（64→3）→ softmax 得动态权重（验证集训练，参数量 &lt;1e4）。

---

## 七、建模方案：档位 C — 三深度子模型 + 多 seed 秩融合

### 7.1 总架构（TFNE-C）

```text
对每个 seed s ∈ {42, 123, 456}（可选 4、5）:
  z^A_s = TFT(X_seq, static)
  z^B_s = FT-Transformer(X_tab)
  z^C_s = NewsLoRA(news)  或 mask

对每个子模型，先在验证集上 winsorize 预测（1%/99%）:
  z̃^m = Winsorize(z^m)

秩融合（静态权重，验证集 ICIR 定）:
  score = Σ_m w_m · Rank( mean_s z̃^m_s )     # 先对 seed 平均再 rank，或先 rank 再平均（二选一，config 固定）

可选动态门控:
  score' = α·score + (1-α)·GateMLP(z^A,z^B,z^C,mkt_vol)
```

**推荐**：**先 per-seed 训练 → 验证集上 seed 等权平均 z → 再 Rank 融合**（Mekelburg pooling 精神）。

### 7.2 子模型 A：Temporal Fusion Transformer（TFT）

| 项 | 规格 |
|----|------|
| 实现 | **pytorch-forecasting** `TemporalFusionTransformer` 或精简自实现 encoder |
| `hidden_size` | 128 |
| `lstm_layers` | 2 |
| `attention_head_size` | 4 |
| `dropout` | 0.2 |
| `max_encoder_length` | 60 |
| 损失 | Huber(`z^A`, `excess_ret_1d`) |
| 优化 | AdamW lr=1e-3，**OneCycleLR**；**AMP fp16** |
| Batch | 按交易日，每步 **2048** 只股票（不足则 padding+mask） |
| 早停 | 验证 **mean IC** 连续 8 epoch 不升 |
| 可解释 | 导出 TFT **variable importance** 图写报告 |

**依据**：Lim et al. (2021, IJF)。

**算力**：单卡约 **12–24h/seed**（全池、2019–2024H1）。

### 7.3 子模型 B：FT-Transformer

| 项 | 规格 |
|----|------|
| 实现 | `rtdl` 的 `FTTransformer` 或 `pytorch-tabular` |
| `d_token` | 128 |
| `n_blocks` | 4 |
| `attention_dropout` | 0.2 |
| `ffn_dropout` | 0.1 |
| 损失/优化 | 同 A |
| Batch | 按日 4096 行 |

**依据**：GKX 高维非线性截面；表格 Transformer 文献。

**算力**：单卡约 **6–12h/seed**。

### 7.4 子模型 C：NewsLoRA

| 项 | 规格 |
|----|------|
| 骨干 | `hfl/chinese-roberta-wwm-ext` |
| LoRA | `peft` LoraConfig, r=8 |
| 训练 | 仅 2019–2024 新闻；max_length=256；batch=32 |
| 损失 | Huber 对 `excess_ret_1d`（链指到股的样本）或辅助情感分类（可选） |
| 学习率 | 骨干 1e-5，LoRA/head 2e-4 |
| 早停 | 验证 IC |

**算力**：单卡约 **8–16h**（可先离线 embedding 再 LoRA 微调 head+LoRA）。

### 7.5 多 seed 集成与消融

| 实验模型 | 说明 |
|----------|------|
| **TFNE-C（主）** | 3 seed × 三模态 + 秩融合 |
| **TFT-only** | 消融时序 |
| **FTT-only** | 消融截面 |
| **NewsLoRA-only** | 消融文本（2019+） |
| **档位 A（RGNE）** | 小 GRU+MLP+冻结 BERT，同流水线对比 |
| **等权 Rank、无 winsorize** | 对照 Mekelburg 2024 |

**融合权重 \(w_m\)**：验证集 2024H2，\(w_m \propto \max(ICIR_m, 0)\)，归一化。

### 7.6 与 2023–2025 新文献的对比讨论（报告「相关工作」可写，主实验不变）

主实验仍锚定 **GKX + TFT/FT-Transformer + NewsLoRA + Mekelburg (2024)**；下列方向仅作**对照与局限说明**，可在实验报告单列一小节「为何不采用 FinLLM 主干」。

| 方向 | 代表工作（示例） | 与本作业的关系 | 本方案取舍 |
|------|------------------|----------------|------------|
| **金融大模型 / FinLLM** | BloombergGPT (Wu et al., 2023)；FinGPT (Yang et al., 2023)；领域继续预训练 + 指令微调类工作 | 强在文本理解与零样本摘要；弱在与**逐日 5000 股截面排序、IC、T+1 回测**的标准化对齐 | 文本支路用 **RoBERTa-wwm + LoRA**，参数与数据可控；不做闭源 API 端到端选股 |
| **多模态融合（价量+新闻+另类）** | 深度学习股价预测综述 (Li et al., 2022, arXiv:2212.12717)；金融多模态/跨模态学习综述 (2023–2025 多篇，见参考文献) | 与 TFNE-C「分模态 + 秩融合」**问题设定一致**，但架构与训练细节各异 | 采用 **TFT + FTT + 注意力池化新闻**，用 Mekelburg 式 **winsorize + 多 seed 秩融合** 替代「单一大模型黑箱」 |
| **通用 LLM 直接预测收益** | ChatGPT/ GPT-4 类金融应用预印本 (2023–2024) | 常为小样本、短窗口或事件研究；**全历史 walk-forward IC** 较少 | 不作为主模型；若做扩展，仅限**小样本事件日**辅助分析，不与主表 IC 混报 |
| **更新时序架构** | PatchTST、iTransformer 等 (2023+) | 可能提升时序分支，但需重新调参与消融 | 主实验固定 **TFT**；有余力可在消融中增 **1 组** 新时序模型，并报告验证 ICIR 是否优于 TFT |

**报告写法建议**：用 1 段说明「新文献提供了什么直觉（如 FinLLM 的事件理解、多模态 attention）」，再用 1 段说明「本方案因**可复现、无泄露、与 PDF 评估一致**而选择表中主依据」；避免只堆新论文标题而无对照实验。

---

## 八、交易策略与组合构建（与 TFNE-C 流水线闭环）

本节是 **第五节预处理 → 第六节特征 → 第七节建模** 的自然延伸：只使用 `04_ensemble_predict.py` 产出的 **`score`** 及面板中**已因果对齐**的字段，不引入新特征、不用测试集调参。同花顺模拟交易按本节规则下单即可。

### 8.0 在流水线中的位置

```text
panel_daily.parquet + features/
  → TFNE-C 训练 / 融合
  → predictions/{trade_date}.parquet   # 列: ts_code, z_A, z_B, z_C, score
  → strategy.target_weights(score, panel_t)   # 本节
  → backtest.simulate(orders)                 # 第九节
```

| 上游模块 | 策略层如何复用 |
|----------|----------------|
| **5.2 标签** | 预测目标为 `excess_ret_1d` → 执行 **\(t\) 收盘信号、\(t+1\) 成交** |
| **5.3–5.4 截面处理** | 股票池与训练样本一致：已剔除 ST/北交所；流动性与特征同源 |
| **6.1–6.2 特征** | 流动性用 `log_amount` 20 日均；行业用 `basic.industry` 做权重 cap |
| **6.4 / 7.1 门控** | 可选：高 `mkt_vol` 时降低股票总仓位（与 TFT 市场通道一致） |
| **7.5 融合** | `score` = 多 seed 秩融合 + **截面 winsorize**（与 Mekelburg 一致） |

---

### 8.1 交易信号 `score` 的定义（与建模输出一致）

对每个 `trade_date = t`、每只股票 \(i\)：

1. 子模型输出 \(z^A_s, z^B_s, z^C_s\)（每个 seed \(s\)）  
2. 截面 winsorize：\(\tilde z^m_{i,s} = \mathrm{Winsorize}_{1\%,99\%}(z^m_{i,\cdot,s})\)  
3. seed 内平均：\(\bar z^m_i = \mathrm{mean}_s \tilde z^m_{i,s}\)  
4. 秩融合（与第七节相同）：  
   \[
   \mathrm{score}_{i,t} = \sum_m w_m \cdot \mathrm{Rank}_{i \in \mathcal{S}_t}\bigl(\bar z^m_{i,t}\bigr)
   \]
   \(w_m\) 在 **2024H2 验证集** 上按 ICIR 估计；\(\mathcal{S}_t\) 为当日可交易集合。

**策略层不再对 `score` 做全样本标准化**；仅允许**当日截面**变换（与 GKX 精神一致，见 8.3）。

---

### 8.2 可投资集合 \(\mathcal{S}_t\)（与面板构建规则一致）

股票 \(i\) 在 \(t\) 日进入 \(\mathcal{S}_t\) 当且仅当：

| 条件 | 数据来源 |
|------|----------|
| 非 ST、非北交所 | `stock_st`, `ts_code` |
| 非停牌 | `daily.vol > 0` 或当日有成交 |
| 上市满 60 日 | `list_date` vs `trade_date` |
| 过去 20 日日均成交额 \(\ge A_{\min}\)（默认 5000 万） | `daily.amount`（千元）滚动均值 ×1000 |
| 有效 `score` | `predictions` |
| **新买入**额外：非涨停 `pct_chg < 9.5%` | `daily` |

**卖出**：持仓中跌停/停牌/T+1 不可卖则当日跳过卖单；`score` 掉出选股域时在可卖后清仓。

---

### 8.3 从 `score` 到目标权重（与 IC 排序目标一致）

IC 用 **Spearman 秩相关**，故组合权重基于 **当日截面秩** 而非 raw 分数绝对值：

#### 步骤 1：选股域

在 \(\mathcal{S}_t\) 内取 `score` **不低于 \(q_{\min}\) 分位数**（默认前 10%）的股票，按分数降序最多保留 \(N_{\max}\)（默认 50）只 → \(\mathcal{U}_t\)。

#### 步骤 2：秩 → 权重（softmax on rank）

\[
r_{i,t} = \frac{\mathrm{rank}(\mathrm{score}_{i,t})}{|\mathcal{U}_t|} \in (0,1], \quad
\tilde w_{i,t} = \frac{\exp\bigl((r_{i,t}-0.5)/\tau\bigr)}{\sum_{j\in\mathcal{U}_t}\exp\bigl((r_{j,t}-0.5)/\tau\bigr)}
\]

\(\tau\) 越小，权重越集中在头部（默认 0.5）。

#### 步骤 3：风险约束（与特征里的行业/市值同源）

1. **单票上限** \(w_{\max}=5\%\)：截断后盈余按权重比例再分配  
2. **行业上限** \(w^{\text{ind}}_{\max}=25\%\)：`basic.industry` 分组缩放（与 6.2 `industry_id` 一致）  
3. **可选市值倾斜**：对 `log_mv` 最低 10% 微盘股权重 ×0.5（与 metric 特征一致，降低流动性风险）

#### 步骤 4：现金缓冲与市场状态

\[
c_t = c_{\min} + \gamma \cdot \mathbb{1}\bigl[ \mathrm{mkt\_vol}_t > q_{80}(\mathrm{mkt\_vol}) \bigr]
\]

默认 \(c_{\min}=3\%\)，\(\gamma=5\%\)（高波动日多留现金）；`mkt_vol` 与 **6.1 TFT 市场通道** 同源（000300 20 日实现波动）。\(q_{80}\) 在**训练+验证期**估计，固定后用于测试。

\[
w^{\mathrm{eq}}_{i,t} = (1-c_t)\cdot \frac{\tilde w_{i,t}}{\sum_{j}\tilde w_{j,t}}, \qquad
\boxed{V^{\mathrm{tgt}}_{i,t} = W_t \cdot w^{\mathrm{eq}}_{i,t}}
\]

\(W_t\)：\(t\) 收盘组合净值（现金 + 持仓市值）。

---

### 8.4 调仓执行（T+1、资金分配）

**时点**：\(t\) 收盘生成目标权重 → **\(t+1\) 开盘价** 成交（±5bps 滑点）；与 `excess_ret_1d` 标签一致。

| 步骤 | 规则 |
|------|------|
| 目标偏离 | \(\Delta V_i = V^{\mathrm{tgt}}_{i,t} - V^{\mathrm{cur}}_{i,t}\) |
| 不调仓带 | \(|\Delta V_i|/W_t < \delta\)（默认 1%）→ 不交易 |
| 股数 | \(\Delta\text{shares}_i = \mathrm{sign}(\Delta V_i)\lfloor|\Delta V_i|/(100P_i)\rfloor\times 100\) |
| 顺序 | **先卖后买**；现金不足时所有买单按 \(\Delta V_i\) **同比缩放** |
| 换手上限 | \(\mathrm{TO}_t=\frac{1}{2W_t}\sum_i|\Delta V_i|\)；若 \(>\Omega_{\max}\)（25%）则全体 \(\Delta V_i\) 同比缩小 |
| T+1 | 买入日 `sellable=False`，次一交易日可卖 |

**每只新买入/加仓花多少钱**：由 \(V^{\mathrm{tgt}}_{i,t}\) 唯一决定；高分股票 \(r_{i,t}\) 大 → \(w^{\mathrm{eq}}\) 大 → 目标金额高。

**数值例**：\(W_t=100\) 万，\(c_t=3\%\)，某股 \(w^{\mathrm{eq}}=4\%\) → 目标市值 **4 万**；第 50 名边缘票 \(w^{\mathrm{eq}}\approx 0.8\%\) → **约 8000 元**。

---

### 8.5 费用与 `backtest` 接口

| 项 | 默认 |
|----|------|
| 佣金 | 双边万 1.5 |
| 印花税 | 卖出千 0.5 |
| 初始资金 | 100 万（`config.yaml`） |
| 输出 | `artifacts/backtest/equity_curve.csv`, `daily_positions.parquet`, `metrics.json` |

```python
# src/strategy.py  — 与 06_backtest.py 对接
def build_orders(score: pd.Series, panel_t, holdings, cash, W, params, mkt_vol) -> list[Order]:
    w = target_weights(score, panel_t, params, mkt_vol)  # 8.3
    return deltas_to_orders(w, W, holdings, cash, params)  # 8.4
```

---

### 8.6 策略超参（仅在验证集 2024H2 选取）

| 参数 | 候选 | 说明 |
|------|------|------|
| \(N_{\max}\) | 30, 50, 80 | 持股数 |
| \(q_{\min}\) | 0.85, 0.90, 0.95 | 分数分位门槛 |
| \(\tau\) | 0.3, 0.5, 1.0 | 权重集中度 |
| \(\delta\) | 0.5%, 1.0%, 2.0% | 不调仓带 |
| \(\Omega_{\max}\) | 20%, 25%, 35% | 换手上限 |

**联合目标**（与第九节一致，在验证集上最大化）：

\[
J = \text{ICIR}_{\text{val}}^{\text{(pred)}} + \lambda_1 \text{Sharpe}_{\text{val}}^{\text{(bt)}} - \lambda_2 \overline{\mathrm{TO}} - \lambda_3 \text{MaxDD}
\]

建议 \(\lambda_1=0.2,\ \lambda_2=2.0,\ \lambda_3=0.5\)。**2025 测试集只评一次**，不调参。最优参数写入 `config.yaml` 的 `strategy:` 段。

> **注意**：\(w_m\)（模态融合）在验证集上估计；策略参数 \((N_{\max},\tau,\ldots)\) 也在验证集估计——二者可用**同一验证段**，但应在报告中分开说明，避免「双重调参」争议（模态权重先固定，再调策略，或嵌套 walk-forward）。

---

## 九、历史回测与评估体系（预测层 + 策略层）

### 9.0 两层评估：不要混用指标

| 层级 | 回答的问题 | 主要指标 | 脚本 |
|------|------------|----------|------|
| **预测层** | 模型排序是否准？ | IC、ICIR、分阶段 IC | `05_evaluate_ic.py` |
| **策略层** | 排序能否变成可交易收益？ | 年化、夏普、最大回撤、换手、成本后夏普 | `06_backtest.py` |

IC 高但夏普低 → 优先检查 **换手/成本/涨停过滤**（第八节），而非继续堆模型。

---

### 9.1 预测层评估（与 5.2、7.5 对齐）

对每个 `trade_date`，在 \(\mathcal{S}_t\) 上计算：

\[
\mathrm{IC}_t = \mathrm{corr}\bigl(\mathrm{score}_{\cdot,t},\ \mathrm{excess\_ret\_1d}_{\cdot,t}\bigr)
\]

（Spearman 或 Pearson，全文固定一种。）

| 指标 | 公式/说明 |
|------|-----------|
| mean IC | \(\mathrm{mean}_t(\mathrm{IC}_t)\) |
| ICIR | \(\mathrm{mean}(\mathrm{IC}_t)/\mathrm{std}(\mathrm{IC}_t)\) |
| 分阶段 | 训练/验证/测试分段汇报；牛熊分段（可选） |
| 子模型消融 | TFT-only / FTT-only / NewsLoRA / TFNE-C 四条 IC 序列 |
| 多 seed | 报告 mean ± std（3 seed） |

**输出**：`artifacts/metrics/ic_summary.json`，IC 时序图。

---

### 9.2 策略层回测（与第八节对齐）

**回测区间**（与 5.5 一致）：

| 集合 | 区间 | 用途 |
|------|------|------|
| 验证 | 2024H2 | 选策略超参、报告样本内表现 |
| 测试 | 2025 | **主 OOS**，论文/报告核心表 |
| 全样本图 | 2019–2025 | 净值曲线可视化（注明哪段未参与调参） |

**组合指标**（`metrics.json`）：

| 指标 | 说明 |
|------|------|
| 年化收益率 | 日收益复利年化 |
| 年化波动率 | 日收益标准差 × \(\sqrt{252}\) |
| 夏普比率 | 超额收益/波动（无风险利率≈0） |
| 最大回撤 | 净值峰值到谷底 |
| 平均换手率 | \(\mathrm{mean}(\mathrm{TO}_t)\) |
| 平均现金占比 | \(\mathrm{mean}(\mathrm{cash}/W)\) |
| 成本后夏普 | 扣除佣金/印花税/滑点后 |

**基准**（数据来自 `market/000300.SH`）：

- 买入持有沪深 300  
- 可选：策略超额 = 策略净值 − 基准净值（同起点 100 万）

**稳健性**（实验 E7）：

- 成本 ×2、×3  
- \(\delta \in \{0.5\%, 2\%\}\)  
- 关闭 `mkt_vol` 降仓规则  

---

### 9.3 指数成分与 `index_weight`（仅评估）

- 全市场选股为主策略；**附加**报告「沪深 300 成分内子样本 IC」：用 `index_weight` 的 **point-in-time** 成分过滤 \(\mathcal{S}_t\)  
- 不用于训练、不用于选股（避免成分股未来信息）

---

### 9.4 模拟赛输出（`07_predict_latest.py`）

1. 用截至 **T-1** 的数据跑特征 + 模型 → `score`  
2. 调用 `build_orders(...)` → 次日建议持仓表  
3. 输出 CSV：`ts_code, target_weight, side, est_shares`（供同花顺手工或导入）

---

### 9.5 报告建议图表（第九节 checklist）

1. IC / ICIR 表 + IC 时序（预测层）  
2. 净值曲线：TFNE-C 策略 vs 沪深 300（测试集加粗）  
3. 回撤曲线、月度收益热力图  
4. 换手率 vs 净值（说明成本敏感性）  
5. 行业权重时间序列（检验 25% cap 是否生效）  
6. TFT variable importance Top-10（模型可解释性）

---

## 十、工程结构与复现

### 10.1 目录结构（档位 C 增补）

```text
pro/
├── config.yaml                    # tier:C; L=60; seeds; strategy; 时间切分
├── artifacts/
│   ├── panel_daily.parquet
│   ├── features/                  # 按日或分区
│   ├── news_emb/
│   └── checkpoints/
│       ├── tft/seed_{s}/
│       ├── ftt/seed_{s}/
│       └── news_lora/seed_{s}/
├── scripts/
│   ├── 01_build_panel.py
│   ├── 02_make_features.py
│   ├── 02b_embed_news.py          # 可选：预计算
│   ├── 03_train_tft.py
│   ├── 03_train_ftt.py
│   ├── 03_train_news_lora.py
│   ├── 04_ensemble_predict.py
│   ├── 05_evaluate_ic.py
│   ├── 06_backtest.py
│   └── 07_predict_latest.py
└── src/
    ├── models/tft_module.py
    ├── models/ftt_module.py
    ├── models/news_lora.py
    ├── ensemble.py
    ├── strategy.py                 # 8.3–8.4 目标权重与下单
    ├── backtest.py                 # 9.2 净值与指标
    └── ...
```

### 10.2 环境

```text
python>=3.9
torch>=2.0
pytorch-lightning>=2.0      # TFT 训练可选
pytorch-forecasting>=1.0    # TFT
rtdl>=0.0.4                 # FT-Transformer
peft>=0.10
transformers>=4.36
pandas>=2.0
pyarrow
numpy
scikit-learn
tqdm
matplotlib
```

### 10.3 算力配置（暂定）

| 资源 | 配置 |
|------|------|
| 云方案 | **long**：16 核 CPU，1 GPU，单次 **72h** |
| 主实验 | 分 **4–6 次** 提交：面板( CPU ) → 新闻( GPU ) → TFT×seed( GPU ) → FTT×seed( GPU ) → 融合回测 |
| 累计 GPU | 约 **150–250h**（2 个月内分次） |
| 本地 | 档位 A（`run_tier_a.py`）或档位 C smoke（`run_smoke_c.py`），见 **第十五节** |

### 10.4 执行命令（分阶段）

```bash
# 阶段 A（CPU，8–16h）
python scripts/01_build_panel.py
python scripts/02_make_features.py

# 阶段 B（GPU，12–24h）
python scripts/02b_embed_news.py
python scripts/03_train_news_lora.py

# 阶段 C（GPU，每个 seed 约 24–40h，可分次）
python scripts/03_train_tft.py --seed 42
python scripts/03_train_ftt.py --seed 42
# 重复 seed 123, 456

# 阶段 D（CPU/GPU，4–8h）
python scripts/04_ensemble_predict.py
python scripts/05_evaluate_ic.py
python scripts/06_backtest.py
```

---

## 十一、实验设计（档位 C）

| 编号 | 实验 | 目的 |
|------|------|------|
| E0 | 全样本 scaler | 泄露对照 |
| E1 | 档位 A（RGNE 小模型） | 同流水线弱基线 |
| E2 | TFT-only × 3 seed | 时序消融 |
| E3 | FTT-only × 3 seed | 截面消融 |
| E4 | NewsLoRA-only | 文本消融 |
| E5 | **TFNE-C 全模态** | **主结果** |
| E6 | 无 winsorize / 无多 seed | 验证 Mekelburg 2024 结论 |
| E7 | 成本敏感性 + 策略参数网格（\(N_{\max},\tau,\delta\)） | 策略层（第八、九节） |

---

## 十二、风险与缓解（档位 C）

| 风险 | 缓解 |
|------|------|
| 72h 单次不够 | 分阶段脚本；面板/新闻缓存后多次提交 |
| TFT 过拟合 | Dropout、早停看 IC、减小 hidden |
| LoRA 过拟合新闻噪声 | 早停；市场层新闻；has_news mask |
| 多 seed 方差大 | 报告 mean±std；融合降方差 |
| OOM | fp16；按日 batch；gradient accumulation |

---

## 十三、参考文献

1. Gu, S., Kelly, B., Xiu, D. (2020). *Empirical Asset Pricing via Machine Learning*. **RFS**.  
2. Lim, B., et al. (2021). *Temporal Fusion Transformers…* **International Journal of Forecasting**. arXiv:1912.09363.  
3. Gorishniy, Y., et al. (2021). *Revisiting Deep Learning Models for Tabular Data*. NeurIPS.（FT-Transformer）  
4. Mekelburg, E., Strauss, J. (2024). *Pooling and winsorizing machine learning forecasts…* **Journal of Empirical Finance**.  
5. López de Prado, M. (2018). *Advances in Financial Machine Learning*.  
6. Kakushadze, Z. (2016). *101 Formulaic Alphas*. arXiv:1601.00991.  
7. Giglio, S., Liao, Y., Xiu, D. (2021). *Thousands of Alpha Tests*. **RFS**.  
8. Hu, E., et al. (2022). *LoRA*. ICLR.（PEFT）  
9. Wu, S., et al. (2023). *BloombergGPT: A Large Language Model for Finance*. arXiv:2303.17564.（FinLLM 对比）  
10. Yang, H., et al. (2023). *FinGPT: Open-Source Financial Large Language Models*. arXiv:2306.06031.（FinLLM 对比）  
11. Li, T., et al. (2022). *Deep Learning for Stock Prediction: A Survey*. arXiv:2212.12717.（多模态/深度学习股价预测综述；引用前请核对版本）  
12. 金融多模态与 LLM 应用综述类（2023–2025，报告「相关工作」检索用，终稿按学校格式核对）：如 arXiv:2306.02136；SSRN / arXiv 检索 *financial large language model survey*、*multimodal stock prediction*。  
13. 课程：`深度学习基础大作业.pdf`；`大作业评分细则.md`。

---

## 十四、方案自检

| # | 要求 | 档位 C |
|---|------|--------|
| (1) | 五模块契合 | 同 v1.0；score 进策略；标签 horizon=1 |
| (2) | 适配数据 | 全附加数据类型已用；index_weight 仅评估 |
| (3) | 可复现 | 分阶段脚本 + config；本地 smoke / 云端全量 |
| (4) | 不必全字段 | 见第四节 |
| (5) | 集成 | TFT + FTT + NewsLoRA + 多 seed 秩融合 |

---

## 十五、三档运行方式（代码与配置）

实现位于 `code/`，通过配置文件与入口脚本切换三档；**checkpoint 与预测结果按 `tier_tag` 分目录存放，互不覆盖**。

### 15.1 三档对照表

| 模式 | 入口 | 模型 | 产物目录 |
|------|------|------|----------|
| **档位 A** | `python scripts/run_tier_a.py` 或 `--config config.tier_a.yaml` | GRU + MLP + 新闻哈希 | `artifacts/**/tier_a/` |
| **档位 C 本地调试** | `python scripts/run_smoke_c.py` 或 `--config config.smoke_c.yaml` | TFT-lite + FTT + NewsLoRA（缩小数据/epoch） | `artifacts/**/tier_c_smoke/` |
| **档位 C 主实验** | `python scripts/run_pipeline.py`（默认 `config.yaml`） | 完整 TFNE-C + 多 seed | `artifacts/**/tier_c/` |
| **档位 C 72h 保证版（推荐上云）** | `python scripts/run_server_c_tfne_72h.py` + `config.server_c_tfne_72h.yaml` | TFT + FT-Transformer + NewsLoRA；Top2000、8 epoch、1 seed | `artifacts/**/tier_c/` |

**说明**：

- **档位 A**：主体实验之外的**基线对照**（实验 E1），用于弱模型对比与流水线排错；非正式主结果。
- **档位 C 本地调试**：架构与主实验相同，仅缩小 `max_stocks`、日期窗、`seq_len`、`max_epochs` 与 seed 数；建议 `use_news_bert: false` 以便本机快速跑通。
- **档位 C 主实验**：`config.yaml` 中 `smoke_test.enabled: false`，`models.seeds: [42, 123, 456]`，`features.seq_len: 60`，在云 GPU 上分阶段执行；**需多个 72h GPU 任务**。
- **档位 C 72h 保证版（2026-05 实测调参，推荐首次上云）**：`config.server_c_tfne_72h.yaml` + `run_server_c_tfne_72h.py`。保留三模态 **Transformer**（TFT-lite / FT-Transformer / NewsLoRA），Top3000→**2000**、epoch 30→**8**、seed 3→**1**、News 子采样 **4 万**，粗算 **单次 GPU 35–50 h** 完成 train+eval。**切换到此版必须重跑 prep**（旧 Top3000 的 `artifacts/` 不可复用）。提交见 `code/docs/USTC_SUBMIT_TFNE_72H.md`。

### 15.3 档位 C 72h 保证版明细（TFNE-C · 实测速度对齐）

**背景：** 服务器 Top3000、seq=60 实测 TFT 约 **2.5–3 h/epoch**；满血 30 epoch×3 模态无法在单次 72h 内跑完。**从旧版切换到 72h 保证版时，必须删除旧 `artifacts/` 并用 Top2000 重跑 prep**（见 `code/docs/USTC_SUBMIT_TFNE_72H.md` §二–§五）。

| 项目 | 满血档位 C | **72h 保证版** |
|------|------------|----------------|
| 时序 | TFT-lite（LSTM+注意力） | **同结构** |
| 截面 | FT-Transformer（4 blocks, d=128） | **同结构** |
| 文本 | RoBERTa-wwm + LoRA | **同结构** |
| 股票池 | Top 3000 | **Top 2000** |
| TFT/FTT epoch | 30 | **8**（patience 3） |
| seed | 42, 123, 456 | **42**（其余另开作业） |
| AMP | true | **false**（防 loss=nan） |
| 粗算 GPU | >120 h | **35–50 h** |

```bash
python scripts/run_server_c_tfne_72h.py prep
python scripts/run_server_c_tfne_72h.py train --seeds 42
python scripts/run_server_c_tfne_72h.py eval
# sbatch: slurm_prep_cpu_tfne_72h.sbatch / slurm_train_gpu_tfne_72h.sbatch
```

**报告写法：** 主表可报 72h 版结果；若后续补跑 seed 123/456，在局限性中说明算力约束。

### 15.4 档位 A 模块明细（非主结果）

在 `config.tier_a.yaml` 或 `config.yaml` 中设 `tier: A` 时启用：

| 模块 | 档位 A（基线） |
|------|----------------|
| 时序 | GRU(→64, L=20) |
| 截面 | MLP(128-64-1) |
| 新闻 | 轻量哈希向量 + 线性头（`use_news_bert: false`） |
| seed | 1（默认 42） |
| 数据 | max_stocks≈500 |

**正式报告主表以 `tier: C`（TFNE-C）为准**；档位 A 写入「Baseline / 工程验证」小节；档位 C smoke 仅作本地验证记录，不替代主实验表。

---

## 十六、云算力 long 方案执行时间表

### 16.1 满血档位 C（多 seed，需多个 72h 任务）

【阶段 0，本地或云 CPU，不占 GPU】解压数据、检查 `config.yaml` 路径。

【阶段 1，CPU 8–16h】`01_build_panel` + `02_make_features` → 产出 `panel_daily.parquet`。

【阶段 2，GPU 12–20h】`02b_embed_news` + `03_train_news_lora`（可先 LoRA 后全量 embedding）。

【阶段 3，GPU 单次 72h 内】`03_train_tft --seed 42` + `03_train_ftt --seed 42`；若超时则下次任务续跑 seed 123、456。

【阶段 4，CPU/GPU 4–8h】`04_ensemble_predict` → `05_evaluate_ic` → `06_backtest`。

【阶段 5，赛前】`07_predict_latest` 仅用截至 T-1 特征，输出次日订单 CSV。

### 16.2 72h 保证版（推荐首次上云，见 §15.3）

| 次序 | 资源 | 时长（粗算） | 命令 |
|------|------|--------------|------|
| 1 | CPU 48h | 20–40 h | **必做** `sbatch scripts/slurm_prep_cpu_tfne_72h.sbatch`（Top2000，不可复用旧 prep） |
| 2 | GPU 72h | 35–50 h | prep 成功后 `sbatch scripts/slurm_train_gpu_tfne_72h.sbatch` |

详细步骤：**`code/docs/USTC_SUBMIT_TFNE_72H.md`**。

---

*本方案 v2.0 暂定档位 C；**首次服务器 GPU 实验推荐 72h 保证版**（`config.server_c_tfne_72h.yaml`）。满血多 seed 结果以云端全量跑通后填入实验报告。*
